# PRD — hajaCheck : AI 기반 시설물 외관 하자 점검 플랫폼

> **문서 버전:** v1.4 · **최종 수정:** 2026-08-18 · 이전 버전 `archive/`

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.4 |
| 변경 이력 | **v1.3: LangSmith 마스킹 레이어 완전 제거(#1534)** — 트레이싱 시 6개 체인 프롬프트 원문 전체(개인정보 포함)를 LangSmith로 전송하도록 정책 변경(재봉님 승인). 마스킹 환경변수·가드 모듈·관련 테스트 제거. 관측 편의 우선순위 상향 / **v1.2: PRD↔테스트시트↔코드 3축 정합성 검사 반영(정정 7건)** — ①FR-9 알림 "폴링(30초)" → **주기 폴링 미구현**(마운트·포커스 refetch만) ②FR-8-B **HF 사용량 카드는 서버 자원 카드로 대체 확정**(#728)·**예산 가드레일 미구현**(후속) ③FR-6·§6 Redis③ **RAG 대화 이력(LangChain Memory) 미구현** — 같은 절의 "멀티턴은 후속"과 모순이던 문장 정정 ④FR-4 `/action`은 **`targetStatus` 포함 5필드이며 상태 전이를 함께 수행**(#1128) — "상태 전이와 분리"의 의미를 엔드포인트 분리로 한정 ⑤FR-1 **이메일 인증코드(6자리) 방식 미구현** → 아이디 찾기 = 사업자번호 대조, 비밀번호 찾기 = **재설정 링크 + 1회성 토큰**으로 정정 ⑥FR-1 비밀번호 재설정 **rate limit = 이메일 축 + 전역 상한 2축, IP 축 미사용** 명시 ⑦FR-1 **"재설정 후 세션 전체 무효화"는 미구현**(범위 외 — 후속 #1318)으로 정정. 근거는 `docs/report/prd-test-code-alignment-2026-08-04.md` / **v1.1: 중간보고 이후 실측 정합(사용자 테스트 주간)** — ①**기업 가입 즉시 자동승인 + 오너 멤버십**(#1324/#1368 — prod 전면 403 장애 복구, 인증 배지는 provenance 기준) ②**`CompanyScopeGuard` 4조건**을 인가 정본으로 명시 ③**Flyway V34 → V40** ④**dev/prod = 동일 VM 내 postgres 인스턴스 2개**(5432 dev / 5433 prod)로 정정 — "전용 VM 이관"은 향후 목표로 분리 ⑤**균열 모델 파인튜닝 교체(`crack_unet_resnet34_v2`) + 등급 밴드 v4 재교정 + 폭·길이 px 측정**, **RAG 벡터+BM25 하이브리드(RRF)** ⑥보고서 **관공서 표준양식·책임기술자 표기**, 점검 상태머신(REVIEWED·REPORTED), **오탐 되살리기**, RAG 문서 삭제·임베딩 상태, 분석 잡 큐 실데이터 등 신규 기능 반영 ⑦오픈이슈 #10·#11 해소·#9 부분해소, **킬스위치·mm 환산·프레이밍 한계 신규 등재**. **버전별 상세는 [변경 이력 (Changelog)] 참조** / **v1.0: 중간보고 기준선 — 구현 실측 전면 정합** — ①**PG 결제를 Out-of-Scope → 토스페이먼츠 샌드박스 구현**으로 전환(구독 전이·예약 하향·미결제 유예 포함) ②**균열 탐지를 YOLOv8-seg 단일 → U-Net + YOLO 하이브리드**로 정정(등급 임계값 v2 재캘리브레이션) ③**Flyway 도입 완료**(V1~V34) ④상담원 전용 콘솔·조치 이력 테이블·공개 플랜 API·LangSmith PII 제외·국세청 실존확인 API 반영 ⑤플랫폼 관리자 메뉴 재구성(대시보드 최상위 개칭·하자 유형·등급 관리 삭제). v0.5는 dev 단계에 머물러 main 배포된 적이 없어 **v1.0에 흡수해 함께 릴리스**한다(v0.5 스냅샷은 팀 판단으로 `archive/`에 예외 보존 — 아래 v1.0 항목 참조). **버전별 상세는 헤더표 아래 [변경 이력 (Changelog)] 참조** |
| 작성 | 김승현 |
| 작성일 | 2026-07-15 (최초 2026-07-02) |
| 프로젝트 기간 | 2026-07-09(목) ~ 2026-08-07(금), 4주 |
| 팀 구성 | 김승현(PM), 정재봉(PL), 김관영, 오영석, 이은석, 허남, 유병현 (7명) |
| 프로젝트명 | **hajaCheck** |

## 변경 이력 (Changelog)

### v1.2 (2026-08-04) — PRD ↔ 테스트 시트 ↔ 코드 3축 정합성 검사 반영

> 3개 스택 테스트 전수 실행(backend 2,111 · ai-server 421 · frontend 1,851) + 시트 항목별 코드 실측 대조 결과,
> **PRD만 stale한 항목 7건**(FR 영역 5곳 — FR-1이 3항목으로 분리)을 코드 기준으로 정정한다. 기능 변경은 없고 문서 정정만 담는다.
> 검사 리포트: `docs/report/prd-test-code-alignment-2026-08-04.md`

| # | 절 | v1.1 기재 | v1.2 정정 | 코드 근거 |
|---|---|---|---|---|
| 1 | FR-9 | `notifications` + **폴링(30초)** | 주기 폴링 **미구현** — 마운트·창 포커스 시 refetch만 | `useNotifications`에 `refetchInterval` 없음 |
| 2 | FR-8-B | HF 호출량·누적 토큰 + **예산 가드레일**(경고 배지·디그레이드 모드) | HF 카드는 **서버 자원 카드로 대체 확정**(HF 사용량 공개 API 부재), 가드레일은 **후속 범위** | `SystemMonitoringResponse` 4종 · `monitoring.types.ts:40-41`(#728) |
| 3 | FR-6 · §6 Redis③ | 대화 이력 관리(LangChain Memory + Redis) | **미구현** — 질의는 무상태 단발. 같은 절 "멀티턴은 후속(YAGNI)"과의 내부 모순 해소 | `session_id`는 스키마 자리만, 체인 미사용 · `RagChatAiRequest` javadoc |
| 4 | FR-4 | `/action`은 상태 전이와 **의도적 분리**, **4필드** | **`targetStatus` 포함 5필드**이며 정방향 규칙으로 **전이 수행**. "분리"는 엔드포인트·계약의 분리로 한정 | `Defect#registerActionResult`(#1128) · `openapi.yaml` |
| 5 | FR-1 | 가입·아이디찾기·비번찾기 **이메일 인증코드 6자리**(`email:verify:*`, TTL 5분, 5회 실패 재발급) | 인증코드 방식 **미구현**. 아이디 찾기 = **사업자번호 + 상호명/대표자명 대조**, 비밀번호 = **재설정 링크 + 1회성 토큰**(해시 저장·Host 미신뢰·rate limit) | `email:verify:` 네임스페이스 0건 · `AccountRecoveryService` · `PasswordResetService` · UT-012~021 |
| 6 | FR-1 | 비밀번호 재설정 **rate limit 축** 미명시 | **대상 이메일 축 + 전역 상한 2축**, **IP 축 미사용**(XFF 위조로 무력 — 2026-07-17 결정) | `PasswordResetService:50-51`(`RATE_LIMIT_EMAIL_KEY_PREFIX`·`RATE_LIMIT_GLOBAL_KEY`) · `contract.md` §Rate-limit |
| 7 | FR-1 | 재설정 완료 후 **기존 세션 전체 무효화**(v0.45~v1.1 계속 기재) | **미구현 — 세션 무효화는 범위 외**. non-indexed 세션이라 재설정 후에도 기존 세션이 살아있다(알려진 한계, 후속 #1318) | `PasswordResetService` javadoc "세션 무효화는 범위 외 … 기존 세션은 살아있다" · `contract.md` "이번 범위에서 제외" |

**표기 원칙**: 미구현으로 밝혀진 문장은 삭제하지 않고 **취소선 + "v1.2 정정" 주석**으로 남긴다 — 왜 그렇게 적혀 있었는지와
현재 사실을 함께 읽을 수 있어야 같은 드리프트가 재발하지 않는다. v0.45 등 과거 변경이력 절은 **역사 기록이라 원문 유지**한다.

**동반 정정**: `docs/api-contract/contract.md`의 "`/action`은 상태 전이를 RESOLVED로 고정" 기술(4번과 세트).
`openapi.yaml`은 이미 `targetStatus`를 반영하고 있어 무수정.

**이번 범위 밖(후속)**: 초대 코드(`invitecode` 패키지) PRD 신규 기술 · 마이페이지 프로필 수정(#1443) ·
결제 진입 사업자 인증 게이트(#1444) · 테스트 시트 정정.

### v1.1 (2026-08-04) — 중간보고 이후 실측 정합 (사용자 테스트 주간)

> v1.0(7/30 중간보고 기준선) 이후 dev에 **90커밋**이 들어갔다. 중간보고~사용자 테스트 구간(§8 8.3~8.6)에서 드러난 실사용 결함 수정과 신규 기능을 실측대로 반영한다.

**1. 기업 가입 자동승인 + 회사 스코프 인가 모델 명문화** · (§2.4·FR-1)

- **가입 즉시 `VERIFIED + APPROVED` 자동승인 + 오너 멤버십 발급**(#1324, 같은 트랜잭션). 배경: 이 경로가 없어 **prod에서 기업 가입 계정이 회사 스코프 API 전부 403**(시설물·점검·하자·보고서·통계) 상태였다(#1368 핫픽스, V38로 기존 회사 소급)
- **사업자 인증 배지 판정 기준 전환**: 전건이 `VERIFIED`가 되므로 `verification_status`로는 실검증분을 구분할 수 없다 → **provenance(`ocr_raw.ntsOutcome`) 화이트리스트** 기준(증명 못 하면 배지 false)
- **`CompanyScopeGuard` 4조건**(APPROVED 멤버십 · user ACTIVE · company APPROVED · verification VERIFIED)을 회사 스코프 인가의 정본으로 FR-1에 명시
- 재검증 배치(#888)도 인가 플래그가 아닌 **provenance 여집합(fail-safe)** 기준으로 전환

**2. 배포 지형 정정 — dev/prod는 "전용 VM"이 아니라 "같은 VM의 DB 분리"** · (§6.1)

- v1.0의 "arm1 = dev 전용, prod는 전용 VM 확보 시 이관"은 현행이 아니다. **arm1 한 대에서 dev·prod 동시 가동**, 분리 축은 **postgres 인스턴스 2개**(host 5432 = `hajacheck_dev` / 전용 컨테이너 5433 = `hajacheck`)
- `hajacheck_prod`라는 DB명은 **미사용**. "전용 VM 이관"은 향후 목표로 현재 상태와 분리 표기
- **배포 전제 추가**: `BUSINESS_REGISTRATION_API_KEY`가 `:-` → **`:?` 강제**(미설정 시 배포 하드실패). 위 1번이 이 키의 보안 성질을 뒤집었기 때문 — 이전 미설정=fail-closed, 이후 미설정=**전면 개방 fail-open**

**3. Flyway V34 → V40** · (§6·§6.3)

- V35(baseline 스탬프 누락 `menus`·`menu_role_access` 복구 — P0 기동실패) · V36(pre-Flyway 수동 증분 누락분) · V37(`usage_counters.lock_version` orphan 제거 — prod 쿼터 500 핫픽스) · V38(회사 자동승인 소급) · V39·V40(`rag_documents` 임베딩 상태)

**4. AI 탐지·검색 고도화** · (FR-3·FR-6·§2.3·§9)

- **균열 모델 파인튜닝판 교체 + 등급 밴드 v4 재교정**(#998) — 현장사진 70장 파인튜닝 `crack_unet_resnet34_v2`, 현장 마스크 GOOD 0%→**58.8%**. 밴드 재교정으로 심각알람 F1 **87.9%**, 3등급 정확도 70.2%→**79.6%**
- **균열 등급 = 2지표 min 합의**(면적비 × BLACKHAT 어두움총량) — "가늘지만 긴" 실금 과대판정 해소
- **균열 폭·길이 px 측정값 구현**(P1 정량 추정의 픽셀 단계) + 박리 내부 균열 오탐 필터. **mm 환산은 미해결**(§11-16)
- **RAG 검색을 벡터+BM25 하이브리드(RRF)로 업그레이드**(#1410/#1420) — 법조문 조번호 질의 대응, RRF `k=1` 실측 튜닝. **검색 품질 평가 하네스**(recall/MRR/nDCG) 신설

**5. 신규 기능 반영(PRD에 항목이 없던 것)**

- **점검(Inspection) 상태머신 6단계** `CREATED→UPLOADING→ANALYZING→ANALYZED→REVIEWED→REPORTED` — 하자 상태머신과 별개 축. 보고서 확정 시 REPORTED 전이(#1381) (FR-4)
- **오탐 삭제 되살리기 + 삭제 사유 조회**(#1400), 재분석 대체분 되살리기 차단(#1402) (FR-4)
- **보고서 관공서 표준양식 PDF 1차 적용**(#1294) + **책임기술자 표기**(#1409) + 하자 **탐지 박스 좌표 배선**(화면·PDF, #1333) (FR-5)
- **로그인 후 비밀번호 변경** `PATCH /api/users/me/password`(#1315/#1316) (FR-1)
- **RAG 문서 삭제**(#1411) · 임베딩 504·거짓 상태 수정(#1393, V39/V40) · bge-m3 **warmup**(#1295) (FR-8-B)
- **시스템 모니터링 분석 잡 큐 실데이터**(#1414) · 플랜 KPI를 **`usage_counters` 실사용량** 기준으로 정정(#1413) (FR-8-B)
- **알림 실제 발행 배선** `ANALYSIS_DONE`/`REVIEW_PENDING`(#1283) + 알림센터 버튼 네비게이션(#1288) (FR-9)
- 시설물 **대표사진**(#1286) · 시설물 상세 "점검 이력" 탭 실 API(#1365) (§4)
- 회차 비교 **PDF 내보내기**(#1373 — v1.0의 "PNG"는 오기) · 하자 목록 **필터 결과 PDF 내보내기**(#1249) (§4)
- 하자 카드 **이미지 단위 그룹화**(#1432) + 하자 상세 모달 **전면 리디자인**(#1450) (§4)
- 폼 로그인 성공 시 `ProtectedRoute`가 보존한 **원래 목적지로 복귀**(#1445) · 고객지원 챗봇 안내문에 **경로 바로가기 버튼**(#1455) (FR-1·FR-7)

**6. 오픈 이슈 정리** · (§11)

- 해소: **#10**(보고서 편집기 실사용 검증 — 8/1~3 실사용 기반 수정으로 수행 완료) · **#11**(시설물 팝업 "결과 검수" — 진입 경로 전환·실 bbox 배선으로 배선 완료)
- 부분 해소: **#9**(법정 표준서식 — 관공서 양식 1차 적용, 잔여 범위 재서술)
- 신규: **#15 킬스위치 5종(#1367)**(사업자 소유·통제 미검증 · BRN 선점 락아웃 · 운영자 수동 차단 API · `/api/admin/**`의 `CompanyScopeGuard` 우회 · FAILED 확정 경보) / **#16 균열 mm 실측 환산**(촬영 규약 팀 합의 필요) / **#17 area_ratio 프레이밍 의존성**(미해결 확정 — 고지 여부 결정 필요) / **#18 V38 소급분 재검증 쿼터 상시 소모**

### v1.0 (2026-07-30) — 중간보고 기준선 · 구현 실측 전면 정합

> v0.5(2026-07-24~28)는 dev 브랜치에만 머물러 **main 배포된 적이 없다**(released 최신 = v0.48). 따라서 v0.5의 변경 내역은 **v1.0에 흡수**되며, 아래 v0.5 항목도 v1.0과 함께 처음 배포된다.
>
> **📌 archive 예외 처리**: 레포 규칙(`CLAUDE.md` §문서 버전 관리)은 "archive에는 실제 released된 버전만" 넣도록 하며 릴리스된 적 없는 버전의 등재를 금지한다. v0.5는 미배포이므로 원칙상 대상이 아니지만, **dev에서 상당 기간 팀의 실질 기준 문서로 쓰였고 v1.0 변경 폭이 커 직전 상태를 대조할 스냅샷이 필요하다는 판단으로 `archive/PRD_hajaCheck_v0.5.md`를 예외 보존한다**(원본 버전 헤더 라인 유지). 따라서 archive에는 released 버전(~v0.48)과 이 예외 1건이 함께 존재한다.

이번 회차에서 새로 정합한 항목:

**1. PG 결제 — Out-of-Scope → 토스페이먼츠 샌드박스 구현** · (§2.3·§2.4·FR-1·FR-8·FR-9)

- v0.5까지 "결제는 Out-of-Scope, '업그레이드 문의' 버튼으로 대체"였으나 **실제로는 토스페이먼츠 결제창 연동이 구현**됐다(`payment` 패키지 · `V20__create_payments`). 문서를 구현에 맞춘다.
- 구독 라이프사이클 파생 기능까지 함께 반영: 청구주기(`V27`) · **예약 하향**(`V30 scheduled_plan_changes`) · **미결제 유예**(`V33 payment_pending_until`)
- 알림 타입 3종 추가: `PLAN_EXPIRED` · `PLAN_DOWNGRADED` · `PLAN_DOWNGRADE_FAILED`
- **범위 한정**: **샌드박스(테스트 키) 구현**이며 실 PG 계약·라이브 키 전환은 미완, 결제창 E2E 실검증도 잔여(§11 오픈이슈)

**2. 탐지 모델 — YOLOv8-seg 단일 → U-Net + YOLO 하이브리드** · (FR-3·§6·§7)

- **균열 = U-Net(`crack_unet_resnet34.pt`)**, 박리박락·철근노출 = YOLO — PR #973에서 균열만 교체(v0.5의 "단일 모델 커버" 서술 폐기)
- 등급 산정 단위도 "이미지당 최대 인스턴스 1개" → **"이미지 전체(레터박스 콘텐츠 영역) 마스크 합"**
- 위 두 변화로 v1 임계값이 과대경고(보통 150장 중 83장이 D·E)를 내 **v2 재캘리브레이션**(AI Hub 아파트 균열 470장 재측정, 2026-07-27)

**3. Flyway 도입 완료** · (§6.3·§11)

- v0.5의 "근본 재발 차단은 Flyway 도입(#359)" 미래형 서술 → **완료(#544)**. `V1~V34` 운영, `ddl-auto=validate`, CI에 baseline 정합 통합테스트

**4. 신규 기능 반영(PRD에 항목 자체가 없던 것)**

- **상담원 전용 콘솔**(`/counsel-console`) — 플랫폼 관리자 콘솔 안의 메뉴가 아니라 **COUNSELOR 역할 전용 제3의 셸**(전용 로그인 + 대기열 + 1:1 채팅). 플랫폼 관리자 쪽은 별도로 "상담 관리"(날짜별 조회·읽기전용 대화 열람) (FR-7·FR-8-B·§4)
- **조치 등록 이력**(`V32 defect_action_logs`) — 조치중 단계에서 여러 번 등록되는 사진·조치내용이 단일 스냅샷 컬럼에 덮어써지던 문제를 append-only 테이블로 해소 (FR-4·§6.3)
- **공개 플랜 카탈로그 API** — 랜딩 요금제가 하드코딩에서 **DB `plans` 조회**로 전환. 요금제의 단일 진실은 이제 DB (§2.4)
- **LangSmith 선택적 제외**(v1.0→v1.3) — OCR 체인만 `tracing_context(enabled=False)`로 완전 제외(개인정보 국외 제3자 제공 법적 근거 불확실, #1534 P1), 나머지 5개 체인은 원문 전송 허용(재봉 승인)
- **국세청 사업자 실존확인 API** — OCR에 더해 진위확인·상태조회 연동(미등록/휴업/폐업 판정, API 미설정 시 fail-open) (FR-1)

**5. 메뉴 구성(IA) 재구성** · (§4·FR-8-B)

- **플랫폼 관리자 — "서비스 통계" → "대시보드"로 개칭 + 최상위 배치**(로고 클릭·인덱스 진입도 대시보드로)
- **플랫폼 관리자 — 하자 유형·등급 관리 메뉴 삭제**: v0.5의 "향후 개발 이관" 서술을 실제 메뉴·라우트 제거로 확정
- **점검 주기 설정 — 시설물 관리 → 점검 관리로 이동**

**6. 오픈 이슈 정리** · (§11)

- 해소: **#4**(상담원 역할 주체 — COUNSELOR 전용 콘솔·스킬 관리 구현으로 "관리자 겸임" 전제 자체가 변경) · **#10**(보고서 편집기 블로커였던 `/analyze` 구현 완료) · **#11**(`FacilityResponse.latestInspectionId` 추가로 선행 조건 해소)
- 재서술: **#8**(ARM 추론 최적화 — U-Net 전환으로 ONNX 전제 변동)
- 신규: 결제 라이브 전환 잔여 과제

### v0.5 (2026-07-24) — 구현 실측 정합

> 스프린트2 진척으로 문서와 코드가 벌어진 지점을 실측대로 정정하고, 미구현·보류는 "후속"으로 태그했다. 담당자별 반영 항목:

**1. 플랫폼 관리자 경로 분리** — 김승현 · (FR-1·FR-8·§4)

- 진입 경로 `/admin` → `/platform-admin` (로그인 `/platform-admin/login`)
- 로그인 성공 계정이 `PLATFORM_ADMIN`이 아니면 거부

**2. 관리자 콘솔 메뉴 재구성** — 김승현 · (FR-8·§4)

- 기업 관리자 = 사용자 관리 · 플랜/쿼터 관리
- 플랫폼 관리자 = 사용자 관리 · 플랜/쿼터 관리 · 상담 관리 · RAG 문서 관리 · 서비스 통계 · 시스템 모니터링
- 하자 유형·등급 기준 관리는 **"향후 개발"로 이관**

**3. 조직 모델 정정** — DB 스키마 · (§2.4·§6.3)

- `companies` · `company_memberships`(초대/승인 워크플로) 테이블 실재 → "별도 companies 테이블 없이" 단순화 서술 폐기
- `facilities` 소유 주체를 회사로 전환(#660)
- 승격 #531 스키마 정합 핫픽스(`lock_version` 낙관적 락 · `rag_documents.verification_status`) 반영

**4. 시설물 등록/점검 관리 실측** — 허남/오영석 · (§4·§6.3)

- 초기등급 · 담당자 · 메모 필드 확장, 주소검색 + Geocoder 좌표 자동화
- `GET /api/facilities/assignable-users` 신규
- 다가오는 점검 전용 페이지 · 회차 비교 KPI/차트/PNG · 점검주기 전체현황 테이블

**5. 하자 관리 재해석** — 유병현 · (§4·FR-4)

- 하자 단건 목록 → **점검(Inspection) 단위 그룹 목록 + 점검 상세 → 하자 상세 모달 2단계**
- 조치 결과 등록(`PATCH /api/defects/{id}/action`) · 활동 기록 타임라인 · `GET /api/inspections` 신규

**6. 지도 뷰·랜딩** — 김관영 · (§2.3·§4)

- 지도 좌표 = 카카오맵 Geocoder(주소→좌표)로 구현 (EXIF GPS 자동추출은 미채택 → §11 오픈이슈)
- 이용약관 · 개인정보처리방침 라우트 신설

**7. AI 보고서/저장 방어로직 실측** — 김관영 · (FR-5)

- 프롬프트 인젝션 방어 · 인용 검증 · 백엔드 프록시 · 이중 Rate Limit · 내부 인증 헤더
- DRAFT/FINALIZED 상태 · 유니크 제약 · PDF 매직넘버 · 낙관적 락 · path traversal 차단

**8. 점검 관리 A · AI 추론(DL) 영역 현실 정정 (불일치 → 후속)** — 오영석·정재봉 · (FR-2·FR-3·§6)

- 영상 프레임 추출 미구현(이미지 전용) · ONNX+INT8 미적용(현재 raw PyTorch)
- FastAPI 워커 단일 프로세스 · 청크 업로드 = 단일 요청 + 진행률 · 점검 생성 담당자 = 본인 자동배정

**9. 상담 내역 통합** — 정재봉 · (§4)

- 마이페이지 · 고객지원 이원화 → **고객지원으로 단일화**

**10. 담당자 이관** — 이은석 · (§7)

- RAG 문서 관리 · 상담 관리 담당 김승현 → **이은석**(고객지원 RAG 챗봇 오너와 통합)

**11. AI 오케스트레이션·관측 도입 (LangGraph·LangSmith)** — 오영석 · (§6·§6.2·FR-5·FR-6)

- **LangSmith 트레이싱**(관측, #717) — `get_llm()` 자동 계측, docker-compose `fastapi`에 env 연결
- **report_chain LangGraph StateGraph**(#703) — grounding 재시도 `while` 루프 → 조건부 엣지 cycle(재시도가 트레이스에 개별 단계로 노출)
- **rag_chat_chain LangGraph StateGraph**(#710) — 단발 파이프라인을 그래프로 감싸고 캐시 히트/검색 0건 조건부 엣지
- 공용 의존성 조합 고정(`langchain 0.3.30`·`langchain-core 0.3.86`·`langsmith 0.10.10`·`langgraph 0.6.11`), `requirements.txt` 변경은 사람 수동 머지

### v0.48 (2026-07-20 이후) — LLM 서빙 확정

- **Ollama 스위치 제거 — HF Serverless Inference(Qwen3-8B) 전담**으로 결정
- GPU 확보 시 로컬 대체 옵션 삭제

### v0.47 (2026-07-20) — 가입/인증 정책 · 관리자 콘솔 분리

- Free 플랜은 관리자 승인 대기 없이 **가입 즉시 체험 가능**하도록 명시
- **사업자등록번호 인증을 가입 시 선택사항으로 전환** — 업그레이드(Free→Standard/Enterprise) 시도 시 미인증이면 인증 절차 후 진행, 인증 완료면 바로 진행 (§2.4·FR-1·FR-8)
- **관리자 페이지를 기업 관리자 / 플랫폼 관리자 콘솔로 분리** — `users.company_id` 기준 기업 간 데이터 스코핑, `/admin` 경로는 플랫폼 관리자 전용 로그인 진입점

### v0.46 (2026-07-17) — 보고서 확정 게이트

- Grounding `PASS` 보고서만 PDF 확정 허용
- `WARN`은 초안 수정 · 재검증 대상으로 명시

### v0.45 (2026-07-15) — 이메일 인증 도입

- 회원가입 · 아이디 찾기 · 비밀번호 찾기 3개 화면에 **이메일 인증(Spring Mail SMTP, 코드 입력형 6자리)** 추가
- Redis 키 `email:verify:{PURPOSE}:{email}`(PURPOSE=SIGNUP/FIND_ID/RESET_PW), TTL 5분, 1회성 삭제, 5회 실패 시 재발급 강제, 재발송 쿨다운 60초
- v0.43의 사업자등록번호 대조 방식은 이메일 인증 코드 방식으로 대체

### v0.44 (2026-07-15) — 요금제 · 프론트 공통 스택 확정

- §2.4 요금제 확정 (Standard 월 ₩29,000 / Enterprise 월 ₩59,000, "협의/별도 견적" 방침 폐기)
- 프론트엔드 공통 스택: **Tailwind CSS 4.3.2 + Recharts 3.9.2**(차트·그래프 공통 컴포넌트, 화면별 혼용 금지)

### v0.43 (2026-07-14) — 아이디/비밀번호 찾기

- FR-1 로그인·마이페이지에 아이디 찾기 · 비밀번호 찾기(재설정) 기능 추가 — 기업회원(ID/PW 로그인) 대상
- **사업자등록번호 기반 본인 인증**(이메일 발송 없음 — 가입 시 OCR로 확인된 사업자등록정보로 동일 세션 내 즉시 인증)
- 시도 횟수 제한 · 인증 성공 후 세션 전체 무효화 원칙 §5 반영

### v0.42 (2026-07-13) — 착수 주간 진척 반영

- 탐지 클래스 5종 → **3종 확정**(균열·박리박락·철근노출)
- 데이터셋 **약 161,000장 확보 완료**
- ONNX 경량화 · 모델 버전관리 규칙 **완료**
- 논리·물리 ERD **완료**
- AI 공통기반(llm_client · prompts · structured output · 법규 청킹 · 예시체인) **완료**
- 심각도 등급 **A~E 5등급 확정**(오픈이슈 해소)
- 랜딩 · 지도뷰 · Stitch→Figma **완료**

### v0.41 (2026-07-07) — 기술 챕터제 · 협업 도구 전환

- §7 기술 챕터 표를 9개(기술 축)로 정리 — 주/부담당은 메뉴 오너에서 도출, 각 챕터 실제 기술 스택 명시(챕터9=DevOps 플랫폼 운영 레이어)
- §8 협업 도구: PACA(자체 호스팅) 폐기 → **Jira · Slack 전환**

> 그 이전(v0.30~v0.37 등) 이력은 `archive/` 스냅샷 참조.

---

## 목차

1. [개요 (Overview)](#1-개요-overview) — 1.1 배경 및 문제 정의 / 1.2 솔루션 요약
2. [목표 (Goals)](#2-목표-goals) — 2.1 프로젝트 목표 / 2.2 목표 사용자 / 2.3 범위 정의(P0·P1·P2) / 2.4 비즈니스 모델(멤버십)
3. [유사 플랫폼 벤치마킹](#3-유사-플랫폼-벤치마킹)
4. [메뉴 구성 (IA)](#4-메뉴-구성-ia)
5. [핵심 기능 요구사항](#5-핵심-기능-요구사항) — FR-1 로그인·마이페이지·결제 / FR-2 업로드 / FR-3 AI 하자 탐지 / FR-4 시각화·검수 / FR-5 LLM 보고서 / FR-6 RAG 챗봇 / FR-7 상담 챗봇·상담원 콘솔 / FR-8 관리자 페이지 / FR-9 알림 센터(P1)
6. [시스템 아키텍처](#6-시스템-아키텍처) — 6.1 배포 환경(OCI) / 6.2 AI 개발 컨벤션 / 6.3 데이터 모델
7. [팀 구성 및 역할](#7-팀-구성-및-역할--메뉴도메인-단위-배정) — 메뉴 담당 / 기술 영역 담당
8. [일정 (Milestones)](#8-일정-milestones) — 코드 리뷰 운영 방식 포함
9. [성공 지표 (KPI)](#9-성공-지표-kpi)
10. [리스크 및 대응](#10-리스크-및-대응)
11. [오픈 이슈 (팀 확정 필요)](#11-오픈-이슈-팀-확정-필요)

**연관 문서**: `AI_개발_컨벤션.md` · `SpringBoot_코드_컨벤션.md` · `React_코드_컨벤션.md` · `팀회의_아키텍처_비교_모놀리식_vs_MSA.md` · `docker-compose.yml`(배포 정의 — 운영·로컬 공용)

---

## 1. 개요 (Overview)

건물 외벽·교량 등 시설물을 촬영한 **이미지/영상을 웹 플랫폼에 업로드**하면, AI(딥러닝 비전 모델)가 균열·박리박락·철근 노출 하자를 자동 탐지하고, LLM(LangChain + RAG)이 점검 보고서 초안을 자동 생성해 주는 시설물 안전점검 지원 플랫폼.

### 1.1 배경 및 문제 정의

- 100m 이상 고층 건물 외벽, 고속도로 교량 하부 등은 사람이 직접 점검하기에 **추락 위험, 고비용, 장시간 소요** 등 부담이 크다.
- 육안 점검은 점검자 숙련도에 따라 판독 편차가 크고, 점검 이력이 체계적으로 축적되지 않는다.
- 국내(뷰메진 VODA, 드론와이드샷)·해외(Inspekt AI, Hammer Missions) 모두 촬영 이미지 기반 AI 시설물 점검 서비스가 상용화 단계에 있어 시장성이 검증된 주제다.

### 1.2 솔루션 요약

1. 촬영된 시설물 이미지/영상 **업로드** (촬영 수단 무관)
2. AI 모델(FastAPI 서빙)이 하자 **자동 탐지·분류·심각도 평가**
3. 결과를 웹 대시보드에서 **시각화**(원본 위 바운딩박스/세그멘테이션 오버레이)
4. LangChain 기반 LLM + RAG(점검 법규·기준 VectorDB)로 **점검 보고서 초안 자동 생성**
5. 점검 이력 축적 및 이전 점검 대비 변화 추적
6. **시나리오 챗봇 + 상담원 연결**로 사용 문의·점검 상담 지원

---

## 2. 목표 (Goals)

### 2.1 프로젝트 목표

- 4주 내 업로드→탐지→시각화→보고서 생성의 **End-to-End 데모 완성**
- 하자 탐지 모델 성능 목표: 균열 탐지 기준 **mAP@0.5 ≥ 0.7** (공개 데이터셋 기준, 팀 협의로 조정)
- 보고서 초안 생성 소요: 이미지 100장 기준 **10분 이내**

### 2.2 목표 사용자 (Persona)

| 페르소나 | 설명 | 핵심 니즈 |
|---|---|---|
| 안전점검 기술자 | 시설물 안전점검 업체 소속, 촬영 결과 판독 | 판독 시간 단축, 누락 방지 |
| 시설물 관리자 | 건물주·관리사무소·도로공사 담당자 | 상태 한눈에 파악, 이력 관리 |
| 점검 책임자 | 보고서 결재·제출 담당 | 법규 기준에 맞는 보고서 신속 작성 |
| 서비스 관리자 | 플랫폼 운영자 | 사용자·콘텐츠·상담 관리 |

### 2.3 범위 정의

**In-Scope (P0 — 필수)**

- 랜딩 페이지(로그인 전 — 서비스 소개·요금제·시작하기)
- 소셜 로그인(Kakao/Google OAuth2) + 마이페이지
- 시설물(프로젝트) 관리
- 이미지 업로드 및 전처리 (영상→프레임 추출은 후속 범위 — v0.5, FR-2)
- AI 하자 탐지: 균열(crack) 중심 + 박리·박락, 철근 노출 — **3종 다중 클래스 확정**(§FR-3)
- 탐지 결과 시각화(오버레이, 확대, 하자별 필터) 및 수동 검수
- 하자 심각도 등급 분류
- LLM 점검 보고서 초안 자동 생성(LangChain) + PDF 내보내기
- RAG 기반 점검 기준 Q&A 챗봇 (LangChain + Chroma)
- 시나리오 챗봇 + 상담원 연결(실시간 채팅)
- 관리자 페이지(사용자·하자 기준·상담 관리)

**P1 — 여유 시 추가**

- 점검 이력 비교(동일 시설물 회차 간 하자 변화 추적)
- 하자 통계 대시보드(유형별/등급별/구역별)
- 균열 폭·길이 정량 추정(픽셀→실측 환산) — **v1.1: 픽셀 단계 구현 완료**(`area_px`·`length_px`·`width_px`, 보고서 표기용). **mm 실측 환산은 미해결**(기준물 촬영 필요 — FR-3·§11)
- **점검 계획·주기 관리**(FMS 패턴): 시설물별 점검 주기 설정, 다음 점검일 도래 대시보드 표시
- **지도 뷰**: 시설물을 지도(Kakao Map)에 마커 표시, 하자 최고 등급별 색상(녹/황/적) — **좌표는 시설물 주소를 카카오맵 Geocoder로 변환**(v0.5 실측, EXIF GPS 자동추출은 미채택 — §11 오픈이슈)
- **인앱 알림 센터**: 헤더 벨 아이콘 + 알림 목록 — 분석 완료, 검수 대기, 상담 답변, 점검일 도래(주기 연계). 인앱 폴링 기반, 이메일·푸시는 제외
- **멤버십 쿼터**: 플랜별 사용량 제한(월 분석 장수 등) 체크 + 요금제 소개 페이지 + 마이페이지 "내 플랜" (§2.4)
- **구독 결제(토스페이먼츠 샌드박스)** — v1.0 신규: 플랜 업그레이드 시 결제창 호출 → 승인(confirm) → 플랜 반영, 결제 이력 조회, 청구주기·**예약 하향**·**미결제 유예** 처리 (§2.4). **샌드박스 한정** — 실 PG 계약·라이브 키 전환은 범위 외(§11)
- **메뉴별 경량 AI 기능** (전원 AI 구현 경험 목적 — 각 프롬프트 체인 1개 수준): 대시보드 AI 주간 브리핑, 하자 AI 설명 패널, 하자 자연어 검색 — **Standard 이상 플랜 전용**(§2.4 프리미엄 전략)

**P2 — 확장 목표 (Stretch)**

- **불법 증축 탐지**: 건축물대장/기준 형상 대비 외형 변화 탐지
- 외벽 3D/파노라마 매핑, 열화상 이미지 분석
- 체크리스트 기반 점검(사전점검 앱 패턴 — AI 중심 흐름과 결이 달라 참고 수준)
- **조치 보드(칸반)**: 하자 상태머신을 칸반 보드로 시각화, 드래그로 상태 전이

**Out-of-Scope**

- 촬영 장비(드론 등) 연동·제어
- 법적 효력을 갖는 정식 안전진단 판정(본 시스템은 **점검 보조 도구**)
- **실 PG 계약·라이브 결제**(v1.0 개정) — 결제 자체는 **토스페이먼츠 샌드박스로 구현됨**(P1, §2.4)이나, 사업자등록 기반 실 계약·라이브 키 전환·실청구는 이번 범위 외. 라이브 전환 전제 조건은 §11 오픈이슈 참조
- 정기 자동결제(빌링키 기반 자동 갱신) — 현재는 결제창 방식의 건별 승인만. 만료 시 유예·하향으로 처리(§2.4)

### 2.4 비즈니스 모델 (멤버십·구독)

**목표 고객(B2B)**: 시설물 안전점검 업체, 건물·자산 관리업체, 지자체 시설 관리 부서

**설계 원칙 — 역할과 플랜의 축 분리**: 역할(Role)은 "무엇을 할 수 있는가"(권한, FR-1), 플랜은 "얼마나·누구와 쓸 수 있는가"(용량·좌석·프리미엄 기능). 두 축은 겹치지 않으며, **좌석(점검자 초대 수)이 두 축을 잇는 다리** — Free는 1인 체험, Standard부터 팀 협업(검수 워크플로)이 열린다.

**요금제(안)** — 과금 축: 시설물 수 + 월 분석 이미지 수 + 점검자 좌석 (실원가인 HF API 토큰·스토리지·분석 컴퓨팅과 비례)

| 플랜 | 월 가격 | 시설물 / 월 분석 | 점검자 좌석 | 보고서 | 상담 | AI 부가 기능* | 대상 |
|---|---|---|---|---|---|---|---|
| Free | ₩0 | 1개 / 50장 | ✕ (1인 계정) | 워터마크 | 시나리오 챗봇만 | ✕ | 체험·개인 |
| Standard (월 구독) | **₩29,000** | 10개 / 1,000장 | 3명 | 정식 PDF | 상담원 연결 | ⭕ | 소규모 점검 업체 |
| Enterprise (월 구독) | **₩59,000** | 무제한 / 협의 | 무제한 | + 데이터 반출 | 우선 응대 | ⭕ | 관리업체·공공 |

*AI 부가 기능 = P1 경량 AI(하자 설명·자연어 검색·주간 브리핑)와 회차 비교 — "핵심 AI 탐지는 전 플랜, AI 편의 기능은 유료" 프리미엄 전략(HF 토큰 원가와 비례)

> **⚠️ 요금제 단일 진실 = DB `plans` 테이블**(v1.0 신규). 위 표는 정책 설명용 스냅샷이며, 랜딩 요금제 섹션은 하드코딩이 아니라 **공개 플랜 카탈로그 API**(비인증 조회)로 DB에서 렌더한다 — 한도·기능 플래그(`max_facilities`·`max_monthly_analyses`·`max_seats`·`has_pdf_watermark`·`has_counselor_access`·`has_ai_addon`)를 바꿀 때는 **DB(시드 마이그레이션)가 기준**이고 이 표는 따라 갱신한다. 문서와 DB가 어긋나면 DB가 맞다.

**조직 모델**(v0.5 정정 — 구현 실측): 초기 "별도 조직 테이블 없이 `company_id`만 공유"로 단순화했으나, 실제 구현은 **`companies`(기업) + `company_memberships`(소속·초대·승인) 테이블**로 확정됐다. `company_memberships`는 `status`(PENDING/APPROVED/REJECTED/REVOKED/EXPIRED) 상태머신으로 **좌석 초대·승인 워크플로**를 관리하며, 사용자당 APPROVED 소속은 1개로 제약(부분 유니크 인덱스). **`facilities`의 소유 주체도 개인이 아닌 회사로 전환**(#660) — 점검 생성·담당자 배정은 회사 소속 한정. 여전히 "계정 소유자 = 플랜 보유자, 소유자가 점검자를 좌석 한도 내 초대"라는 §2.4 과금·좌석 원칙은 유지되지만, 그 구현 그릇이 컬럼 공유가 아니라 정규 테이블이라는 점을 반영

**가입 즉시 오너 멤버십·회사 승인**(v1.1 신규): 위 조직 모델은 **멤버십이 실제로 발급돼야** 동작한다. v1.0까지는 가입 시 `User`·`Company`·동의·플랜만 저장하고 **멤버십 발급·회사 승인 경로가 없어**, `CompanyScopeGuard`(FR-1) 4조건을 아무도 만족하지 못해 **prod에서 기업 가입 계정이 회사 스코프 API 전부 403**이었다(#1324/#1368). v1.1부터 가입과 **같은 트랜잭션에서** 회사 `VERIFIED+APPROVED` 전이 + **오너 멤버십(APPROVED)** 발급을 수행한다. 기존 회사는 V38로 소급(REJECTED·FAILED 제외, 멱등)

**구현 범위**: 플랜 테이블·사용량 쿼터 체크(`QuotaInterceptor` — 역할 체크와 분리)·좌석 관리·요금제 페이지·마이페이지 플랜 표시까지 P1, 쿼터/좌석 초과 시 업그레이드 안내 모달 표준 UX. **결제는 v1.0에서 토스페이먼츠 샌드박스로 구현**(아래 참조 — v0.5까지의 "업그레이드 문의 버튼 대체" 방침 폐기)

**구독 결제 — 토스페이먼츠 샌드박스**(v1.0 신규): 플랜 업그레이드는 문의 버튼이 아니라 **실제 결제 플로우**로 처리한다.

- **플로우**: 주문 생성(서버가 주문 식별자·금액 확정) → 토스 결제창 호출(SDK) → 리다이렉트(성공/실패) → **서버 승인(confirm)** → 플랜 반영 + 결제 이력 기록. 금액·플랜은 **서버가 확정한 주문 기준**으로만 승인한다(클라이언트 전달 금액 신뢰 금지)
- **구독 라이프사이클**: 청구주기 관리 · **예약 하향**(상위→하위 변경은 잔여 기간을 보장하고 다음 주기에 반영) · **미결제 유예**(유료→유료 하향 시 결제 실패해도 즉시 강등하지 않고 유예 기간 부여, 킬 스위치 기본 닫힘) · 만료 시 하향. 각 전이는 알림으로 통지(FR-9)
- **범위 한정**: **샌드박스(테스트 키)** 구현이며 실 PG 계약·라이브 키 전환은 Out-of-Scope. 라이브 전환 시 "PG에는 승인됐으나 응답만 유실된 결제"의 복구 경로가 선행 필수 — 미비 상태로 전환하면 실청구를 은폐할 수 있다(§11 오픈이슈)
- 키는 전부 env 주입(`.env`), 미설정 시 **결제 진입점만 비활성**되고 서비스 전체 배포는 성공하도록 소프트 가드(결제는 부가기능이라는 판단)

**Free 즉시 체험**(v0.47 신규): Free 플랜은 관리자 승인 대기 없이 **회원가입 완료(이메일 인증 통과) 즉시 활성화** — 사업자등록번호 인증 여부와 무관하게 바로 체험 가능

**사업자등록번호 인증 — 가입 시 선택, 업그레이드 시 게이트**(v0.47 신규, **v1.0 개정**): 회원가입 시 사업자등록번호 인증은 **선택사항**(미인증 상태로도 가입·Free 이용 가능, `users.business_verified = false`). Free → Standard/Enterprise **업그레이드 시도 시** `business_verified` 확인 → **false면 사업자등록번호 인증 절차로 유도**(인증 통과 후 업그레이드 진행), **true면 바로 진행**. **v1.0**: 결제가 실제로 구현됐으므로 이 게이트는 "문의 접수 전 확인"이 아니라 **결제 진입 전 게이트**로 작동한다

**사업 목표(가설)**: 점검 1건당 판독 인건비 대비 절감 효과를 근거로 Standard(월 ₩29,000) 전환을 유도하고, 팀·다지점 운영 고객은 Enterprise(월 ₩59,000)로 유도 — 완료보고 시 원가·손익 시뮬레이션 1장 포함(v0.44: 고정가 확정으로 시뮬레이션 기준 단가 확보)

---

## 3. 유사 플랫폼 벤치마킹

| 플랫폼 | 국가 | 참고 포인트 |
|---|---|---|
| 뷰메진 VODA | 국내 | 외벽 이미지 기반 2D/3D 가상화 + AI 자동 결함 탐지 |
| 드론와이드샷 | 국내 | 하자 유형 분류 + 분석 보고서 제공 서비스 모델 |
| Inspekt AI | 해외 | 대시보드에서 자산 상태 평가, 회차별 비교 기능 |
| Hammer Missions | 해외 | 탐지 결과에 어노테이션 추가·팀 공유, 보고서 자동 생성 |
| FMS(시설물통합정보관리시스템) | 국내 공공 | 시설물 제원·이력 → **점검계획 수립** → 진단 → 보수·보강의 표준 업무 흐름 (점검 주기 관리 P1의 근거) |
| 자이홈·채들 등 하자접수 앱 | 국내 | 하자 **접수→처리중→보수완료 상태 추적** + 관리자 실시간 공유 (하자 상태머신의 근거) |

공통 패턴: **자산(시설물) 단위 관리 → 점검 회차 → 이미지 뷰어(오버레이) → 결함 목록 → 자동 보고서** 흐름. hajaCheck 메뉴 구성도 이 흐름을 따른다.

---

## 4. 메뉴 구성 (IA)

```
🏠 랜딩 페이지 (로그인 전)
 └ 서비스 소개, 핵심 기능, 요금제(§2.4), 시작하기(로그인 유도)

🔔 알림 센터 (P1 — 전역 헤더 벨 아이콘)
 └ 분석 완료 / 검수 대기 / 상담 답변 / 점검일 도래 — 인앱 폴링, 읽음 처리
     · v1.1: 분석 완료 시 `ANALYSIS_DONE`·`REVIEW_PENDING` **실제 발행 배선**(#1283) + "결과 보기/검수하기" 버튼 네비게이션 연결(#1288)

🔐 로그인/회원가입
 └ Kakao / Google 소셜 로그인

📊 대시보드
 ├ 전체 시설물 현황, 최근 점검 요약, 하자 등급 분포, 처리 대기 현황(검수 대기·조치 대기 건수)
 │   · 등급 분포는 하자 0건 시 '0% × 5등급'으로 오표시되던 엣지케이스 방어(v0.5 실측)
 ├ 다음 점검일 도래 시설물 표시 (P1 — 점검 주기 연계)
 │   └ 다가오는 점검 전용 페이지 `/dashboard/upcoming-inspections`(v0.5 신규 — 배너·D-DAY 리스트·딥링크)
 ├ 회차 간 비교 (P1 — KPI 4종 + 추이 라인차트 + **PDF 내보내기**, v1.1: v1.0의 "PNG"는 오기 — #1373)
 │   · '시각적 비교'에 회차별 대표 사진 표시(v1.1) · '진행성 균열 추이' 차트는 제거(v1.1, #1348)
 │   · 미분석(CREATED/UPLOADING/ANALYZING) 회차는 비교 대상에서 제외(v1.1)
 └ AI 주간 브리핑 카드 (P1 — LLM 자연어 요약) — **메뉴 항목이 아니라 대시보드 안의 인라인 위젯**(v1.2 정정, #1538). 별도 화면이 아니어서 사이드바에 메뉴를 두지 않는다(서버 `menus`·SideNavBar 폴백 양쪽 모두 없음). `/dashboard/ai-weekly-briefing`으로 진입하면 위젯 위치로 스크롤한다(#478)

🏢 시설물 관리
 ├ 시설물 목록/등록 (건물·교량, 위치, 규모, 준공연도, **초기등급·담당자·메모** — v0.5 등록 필드 확장, **대표사진 선택** — v1.1)
 │   · 등록 모달: 우편번호 API 주소검색 + Geocoder 좌표 자동 산출(v0.5 실측)
 │   · 입력 검증: 준공년도 1900~현재+1, 점검주기 ≤120개월(v0.5)
 │   · 담당자 배정: `GET /api/facilities/assignable-users`(회사 소속 INSPECTOR/ADMIN만) — v0.5 신규 API
 │   · 목록 500건 상한 방어(초과 시 실보유건수 WARN 로그, 응답 계약 비파괴)
 ├ 시설물 상세 (기본정보, 점검 이력 타임라인)
 │   · v1.1: "점검 이력" 탭 실 API 배선(#1365) — 목 데이터 폐기. 문서 탭·미배선 버튼은 제거(#1349)
 │   · v1.1: 하자 오버레이가 고정 placeholder 대신 **실 bbox** 표시(#1370)
 │   └ 회차 간 하자 변화 비교 (P1 — 이전 점검 대비 추적)
 └ 지도 뷰 (P1 — 마커, 하자 최고 등급별 색상)
     · 좌표 = **카카오맵 Geocoder(주소→좌표 변환)**로 구현(v0.5 실측 — EXIF GPS 자동추출은 미채택, §11 오픈이슈)
     · 마커 클릭 O(1) 조회, 일괄 재-geocoding 중 주소 변경 시 전 필드 최신값 재적용(lost update 방어)

🔍 점검 관리
 ├ 점검 주기 설정 (P1 — FMS 패턴, 다음 점검일 자동 계산)
 │   └ 점검주기 전체현황 테이블 (v0.5 — 7컬럼, D-day 상태 뱃지, 접근성)
 ├ 점검(회차) 생성 — 시설물 선택, 점검일 (담당자는 **로그인 본인 자동 배정** — v0.5 실측: 별도 담당자 선택 UI는 축소)
 ├ 촬영 데이터 업로드 — 이미지 일괄 업로드, 진행률 표시 (영상은 후속 — FR-2)
 ├ AI 분석 실행/상태 — 분석 큐, 진행 상황
 ├ 분석 결과 뷰어 ★핵심 화면
 │   · 원본 이미지 + 하자 오버레이(바운딩박스/마스크)
 │   · 하자 유형·등급 필터, 확대/이동
 │   · 탐지 결과 수동 확인·수정(오탐 제거, 등급 조정, 누락 추가)
 │   · AI 하자 설명 패널 (P1 — 선택 하자 원인·조치 LLM 생성)
 └ 보고서 생성 진입점 — 회차 상세에서 바로 생성 (벤치마킹 표준 흐름: 점검 회차의 산출물)

📋 하자 관리 (v0.5 재해석 — 하자 단건 목록에서 점검 단위 그룹으로 재구성, 2026-07-24 확정)
 ├ 목록 = **점검(Inspection) 단위 그룹 목록** — 로우가 점검일·시설물·하자건수·등급분포 (`GET /api/inspections` 신규)
 ├ 점검 상세 = 카드형 하자 그리드 + KPI 4종 헤더 → **하자 상세 풀스크린 모달**(2단계 진입)
 │   · v1.1: 하자 카드를 **이미지 단위로 그룹화**(#1432) — 한 사진에서 나온 하자들이 흩어지지 않게
 │   · v1.1: 하자 상세 모달 **전면 리디자인**(#1450 — 정보 위계·갤러리 선택·조치폼) + 이미지 3:4 비율·회차 표기(#1416)
 ├ 자연어 검색 (P1 — 질의를 LLM이 필터 조건으로 변환, 데이터 질의 단일 창구)
 ├ **필터 결과 PDF 내보내기** (v1.1 — #1249. 목록에 있던 보고서 버튼은 제거, 보고서 생성은 점검 회차 진입점으로 일원화)
 ├ 조치 보드 (P2 — 칸반, 드래그로 상태 전이)
 └ 하자 상세 모달 (이미지, 위치, 등급, 조치 상태, 메모)
     · 상태머신: 신규(AI탐지) → 검수확정 → 조치중 → 조치완료 (하자접수 앱 패턴 · V22에서 4단계로 축소)
     · **조치 결과 등록** 폼(조치후사진·조치내용·조치일·담당자) — `PATCH /api/defects/{id}/action`(상태전이 `/status`와 의도적 분리, v0.5 신규)
     · 담당자 드롭다운은 `assignable-users` API 재사용(회사 소속·cross-company IDOR 검증)
     · **활동 기록 타임라인** — `defect_revisions`(감사 이력)를 상태변경·담당자배정·조치내용으로 사용자 노출

📄 보고서 (전체 이력·편집 중심 — 생성은 점검 회차 상세에서)
 ├ 보고서 생성 (점검 회차에서 진입) / 조회(목록·이력) / 편집·출력 — 사이드바 3구분(v0.5)
 ├ 보고서 편집·미리보기 (DRAFT/FINALIZED 상태, "AI 초안이며 법정 제출용 아님" 고지 문구)
 │   · v1.1: 저장/확정검증 통합 + 임시저장·미리보기 가드 개선(#1341), **책임기술자 표기**·결과 요약 정합(#1409)
 └ PDF 내보내기 (Grounding PASS만 확정 가능, 매직넘버 검증·소유권 재검증)
     · v1.1: **관공서 표준양식 1차 적용**(#1294) + 하자 **탐지 박스 좌표 배선**(화면·PDF 공용, 사진 중복 제거 — #1333)

💬 고객지원
 ├ AI 어시스턴트 — 점검 기준·법규 Q&A (RAG 전담. 데이터 질의는 하자 관리 자연어 검색으로 일원화)
 ├ 상담 챗봇 — 시나리오 기반 안내(버튼 플로우) → 상담원 연결(실시간 채팅)
 │   · 진입: 전역 플로팅 버튼(우하단) — 메뉴는 이력 조회 중심
 └ 내 상담 내역 (v0.5 — 마이페이지와 이원화하지 않고 고객지원으로 단일화)

👤 마이페이지
 ├ 내 정보 (프로필, 연동 계정 관리, **비밀번호 변경** — v1.1)
 ├ 내 점검 이력 / 내 보고서
 └ 내 플랜 (P1 — 멤버십 플랜·사용량 표시, **업그레이드 결제(토스 결제창)·결제 이력·예약 하향 확인/취소** — v1.0)
     · 결제 성공/실패 리다이렉트 페이지 → 서버 승인(confirm) 후 플랜 반영
     (내 상담 내역은 고객지원으로 통합 — v0.5, 정재봉)

📈 통계 (P1)
 └ 시설물별·기간별 하자 추이, 회차 간 비교

🛠 관리자 페이지 (기업 관리자 전용, `company_id` 기준 자기 기업 데이터만 조회 — v0.5 메뉴 재구성)
 ├ 사용자 관리 (자기 기업 회원 목록, 권한 변경, 상태 변경(활성/정지))
 └ 플랜·쿼터 관리 (내 플랜 변경, 사용량 조회, §2.4)

🛠 관리자 페이지 (플랫폼 관리자 전용, `/platform-admin` 별도 로그인 — v1.0: 메뉴 재구성, 대시보드 최상위)
 ├ 대시보드 (v1.0 — 옛 "서비스 통계"를 개칭·**최상위 배치**. 가입자·분석 건수·상담 건수 등 전사 지표)
 │   · 콘솔 기본 진입점 — 로고 클릭·인덱스(`/platform-admin`) 진입 모두 이 화면으로
 ├ 사용자 관리 (전체 기업/회원 목록, 권한 변경, 상태 변경(활성/정지))
 ├ 플랜·쿼터 관리 (P1 — 사용자별 플랜 변경, 사용량 조회, 결제 이력·사업자 인증 상태, §2.4)
 ├ 상담 관리 (v1.0 재정의 — **날짜별 상담 티켓 조회 + 읽기전용 대화 열람**. 실시간 응대는 상담원 콘솔로 분리)
 ├ RAG 문서 관리 (법규·지침 업로드/재임베딩 배치)
 └ 시스템 모니터링 (헬스체크, 분석 잡 큐, 에러 로그, 서버 자원, HF 사용량·예산 가드레일)
 · 하자 유형·등급 기준 관리 — **메뉴·라우트에서 삭제**(v1.0 — v0.5의 "향후 개발 이관"을 실제 제거로 확정)

🎧 상담원 콘솔 (v1.0 신규 — `/counsel-console`, COUNSELOR 역할 전용 독립 셸)
 └ 전용 로그인(`/counsel-console/login`) → 상담 대기열 → 1:1 실시간 채팅(WebSocket/STOMP) + 고객 정보 패널
     · 일반 사용자 셸·플랫폼 관리자 셸과 **분리된 제3의 셸** — 상담원은 점검·시설물 기능에 접근하지 않는다
     · 상담원 스킬(이용 방법/분석 결과/결제·기타)별 배정 — 스킬은 플랫폼 관리자가 관리
```

---

## 5. 핵심 기능 요구사항

### FR-1 소셜 로그인 & 마이페이지

- Kakao / Google OAuth2 로그인 (Spring Security OAuth2 Client)
- 세션 관리: Spring Session + Redis
- 마이페이지: 프로필 조회·수정, 소셜 계정 연동 관리, 내 점검 이력·보고서·상담 내역 조회, **내 플랜·사용량 표시 + 점검자 좌석 초대 관리(P1, §2.4)**
- 역할(Role): 일반 사용자 / 점검자 / 관리자 / 상담원 / **플랫폼 관리자**(v0.47 신규) — 계층: `ADMIN > INSPECTOR > USER`, `COUNSELOR`는 별도 축(관리자 겸임 가능). **`PLATFORM_ADMIN`은 위 계층과 분리된 별도 축**(B2B SaaS 플랫폼 운영진 전용, 기업 소속이 아니므로 `company_id` 없음) — 기존 `ADMIN`은 이제 **기업 관리자**(자기 `company_id` 범위 내 전체 권한)로 재정의
- **역할별 권한 매트릭스**:

| 기능 | 일반 사용자 | 점검자 | 상담원 | 기업 관리자 | 플랫폼 관리자 |
|---|---|---|---|---|---|
| 시설물 등록·조회 | 본인 소유만 | 배정 시설물 | ✕ | 전체(자기 `company_id`) | ✕ (운영 대상 아님) |
| 업로드·분석 요청 | ⭕ (플랜 쿼터 내) | ⭕ | ✕ | ⭕ (자기 `company_id`) | ✕ |
| **검수(오탐 수정·등급 확정)** | ✕ | ⭕ 핵심 권한 | ✕ | ⭕ (자기 `company_id`) | ✕ |
| 하자 상태 전이 | 조치 완료 보고만 | ⭕ | ✕ | ⭕ (자기 `company_id`) | ✕ |
| 보고서 생성·편집 | 열람·다운로드만 | ⭕ | ✕ | ⭕ (자기 `company_id`) | ✕ |
| 상담원 콘솔 | ✕ | ✕ | ⭕ 전용 | ✕ | ⭕ (전체) |
| 기업 관리자 페이지 | ✕ | ✕ | ✕ | ⭕ 전용(자기 `company_id`) | ✕ |
| 플랫폼 관리자 페이지(`/platform-admin`) | ✕ | ✕ | ✕ | ✕ | ⭕ 전용(전체 기업) |

- **권한 집행 원칙**: 프론트 메뉴 숨김(`ProtectedRoute`/`AdminRoute`)은 편의일 뿐 — 백엔드에서 `@PreAuthorize`(역할) + 소유권 데이터 필터를 반드시 병행. 플랜 제한은 별도 `QuotaInterceptor`로 분리(§2.4)
- **회사 스코프 인가 정본 — `CompanyScopeGuard` 4조건**(v1.1 명문화): 회사 소유 리소스(시설물·점검·하자·보고서·통계·대시보드) 접근은 아래 **4개를 모두** 만족해야 통과한다. ①요청자에게 **APPROVED 멤버십**이 있을 것 ②`user.status = ACTIVE` ③`company.status = APPROVED` ④`company.verification_status = VERIFIED`
  - **왜 명문화하는가**: v1.0까지 이 4조건을 **만들어 주는 코드 경로가 없어서**(`CompanyMembership.approvedOwner()`·`Company.approve()` 호출부 0건) prod에서 **기업 가입 계정이 회사 스코프 API 전부 403** — 가입 직후부터 아무 기능도 못 쓰는 상태였다(#1324/#1366, 핫픽스 #1368). 인가 조건은 "검사하는 코드"와 "만족시키는 코드"가 **쌍으로** 존재해야 한다
  - ⚠️ **알려진 우회**: `/api/admin/**`에는 `CompanyScopeGuard`가 적용되지 않아 **차단된 회사의 ADMIN도 사용자·플랜 관리에 접근 가능**하다 — 후속 #1367(§11)
- **기업 가입 자동승인 + 오너 멤버십**(v1.1 신규): 기업 회원가입 시 **같은 트랜잭션에서** `Company`를 `VERIFIED + APPROVED`로 전이하고 가입자에게 **오너 멤버십(APPROVED)**을 발급한다(`Company.autoApprove()` — 운영자 수동 승인 `approve()`의 VERIFIED 선행 가드는 별도 보존). 기존 회사는 V38로 소급(REJECTED·FAILED 제외, 멱등)
  - **인증 배지 판정 기준 전환**: 자동승인으로 `verification_status`가 전건 VERIFIED가 되므로, 이 컬럼으로는 "국세청 실검증분"을 구분할 수 없다 → 배지는 **provenance(`ocr_raw.ntsOutcome`) 화이트리스트** 기준으로 판정한다(`VERIFIED`·`LEGACY_VERIFIED`만 배지 ON, **증명 못 하면 false**)
  - **재검증 배치도 provenance 기준**: 자동승인이 가입을 즉시 VERIFIED로 만들어 PENDING만 조회하던 재검증 배치(#888)가 무력화됐다 → 대상 판정을 인가 플래그가 아니라 **provenance 화이트리스트의 여집합**(fail-safe)으로 전환. 국세청 장애 구간 가입분이 사후 탐지에서 이탈하지 않게 한다
- **CSRF 방어**: 쿠키 세션 기반이므로 상태 변경 요청(POST/PUT/PATCH/DELETE)에 Spring Security CSRF 토큰 적용 — React는 `XSRF-TOKEN` 쿠키를 읽어 헤더로 전송
- **기업회원 ID/PW 로그인**(v0.43 신규): 사업자등록증 OCR 인증으로 가입한 기업회원은 소셜 로그인 대신 아이디/비밀번호 자체 로그인 사용(스토리보드 AUTH-01 기업회원 탭). 비밀번호는 BCrypt 해시 저장, 평문 저장·로그 노출 금지
- **가입 시 사업자등록번호 인증 선택제 + Free 즉시 체험**(v0.47 개정): 사업자등록번호 인증(OCR)은 **가입 시 선택사항**으로 전환 — 미인증 상태로도 가입 및 Free 플랜 즉시 이용 가능(승인 대기 없음). 인증 여부는 `users.business_verified`로 관리하며, 유료 플랜 업그레이드(결제 진입) 시점에 게이트로 작동(§2.4)
- **사업자등록증 OCR + 국세청 실존확인 API**(v1.0 실측 반영): 사업자등록증 이미지에서 사업자등록번호·대표자명·상호를 **OCR로 추출**(FastAPI 체인)하고, 국세청 API로 **진위확인(대표자명·개업일 대조) + 상태조회(미등록/휴업/폐업 판정)**를 수행한다.
  - **판정 결과에 따른 분기**: 진위 불일치·휴업·폐업·미등록 → 인증 실패(가입 차단). **API 키 미설정·외부 장애 → fail-open**(`SKIPPED`)으로 `business_verified = false` 상태로 가입은 계속 진행 — 외부 의존성 장애가 가입 자체를 막지 않게 한다. "인증 선택제"(위 항목)는 이 fail-open과 미인증 가입 허용을 뜻하며, **실제 불일치가 검출된 경우는 통과시키지 않는다**
  - 사업자등록번호는 하이픈 제거 정규화 후 저장·중복검사
- **로그인 후 비밀번호 변경**(v1.1 신규): `PATCH /api/users/me/password` — 마이페이지 "내 정보"에서 현재 비밀번호 확인 후 변경(#1315/#1316). 기존 "비밀번호 찾기"(비로그인 재설정)와 별개 경로
- **기업 회원가입 입력 검증 UX**(v1.1): 필수항목(주소·상호명·대표자명) 누락 시 **인라인 에러 + 첫 무효 필드로 스크롤 포커스 + 요약 알림**(#1332) — 어느 항목이 빠졌는지 모른 채 제출이 막히던 문제 해소
- **구독 결제**(v1.0 신규): 마이페이지 "내 플랜"에서 업그레이드 시 토스페이먼츠 결제창 호출 → 서버 승인 → 플랜 반영. 결제 이력 조회, 예약 하향 확인·취소 제공(§2.4)
- **플랫폼 관리자 로그인**(v0.47 신규, **v0.5: 경로 `/admin` → `/platform-admin` 분리**): `/platform-admin/login` 접속 시 기업회원·일반 로그인 화면과 **분리된 플랫폼 관리자 전용 로그인 페이지** 노출. 로그인 성공 시 계정 역할이 `PLATFORM_ADMIN`이 아니면 **로그인 거부**(기업회원·일반 사용자는 이 진입점으로 접근 불가). 플랫폼 관리자 계정은 자체 가입 없이 **초기 시드 데이터로 생성**(운영진 발급), ID/PW 로그인 + Spring Session
- **기업회원 가입 인증 수단(v1.2 정정)**: 가입 검증은 **사업자등록증 OCR + 국세청 진위확인**(위 항목)으로 완결된다. v0.45가 기술한 **가입 이메일 인증코드(6자리) 방식은 구현되지 않았다** — 코드베이스에 `email:verify:*` Redis 네임스페이스도, 대응 서비스 클래스도 존재하지 않는다
- **아이디 찾기(v1.2 정정 — v0.43 대조 방식이 현행)**: **사업자등록번호 + 상호명/대표자명 대조**로 계정을 찾아 **마스킹된 이메일**을 반환한다(`h***@c***.com`, `EmailMasker`). 매칭 실패는 어느 항목이 틀렸는지 구분하지 않고 **항상 동일한 `AUTH_ACCOUNT_NOT_FOUND`**로 응답해 계정 열거를 막는다. v0.45의 "이메일 인증코드 방식으로 대체" 기술은 무효 — 대체는 일어나지 않았다
- **비밀번호 찾기(v1.2 정정 — 코드 입력형이 아니라 재설정 링크)**: 이메일 입력 → **재설정 링크 메일 발송**(Spring Mail SMTP) → 링크의 **1회성 토큰**으로 새 비밀번호 설정. 규약:
  - 토큰은 Redis에 **`sha256(token)` 해시로 저장**(덤프에 원문이 남지 않음), 원문은 메일로만 나간다. **소비 즉시 무효화**(재사용 시 `AUTH_RESET_TOKEN_INVALID`)
  - **계정 열거 방지**: 미가입 이메일도 동일한 200 응답(발송 없음). **소셜 전용 계정**(비밀번호 없음)도 계정 상태를 노출하지 않고 동일하게 거부
  - **Rate limit**(**대상 이메일 축 + 전역 상한** 2축, `PasswordResetService.enforceRateLimits`) 초과 시 `AUTH_TOO_MANY_REQUESTS`. **IP 축은 미사용**(2026-07-17 결정 — nginx `X-Forwarded-For` 위조로 무력화되어 미채택, `contract.md` 비밀번호 재설정 §Rate-limit과 동일)
  - 링크 도메인은 요청 `Host`/`X-Forwarded-Host`를 **신뢰하지 않고 설정값(`frontendBaseUrl`)만** 사용 — 헤더 위조로 피싱 링크가 발송되는 경로 차단
  - ~~재설정 완료 후 기존 세션 전체 무효화(재로그인 요구)~~ → **(v1.2 정정) 세션 무효화는 범위 외 — 재설정 후에도 기존 세션은 유지된다.** 현 설정이 non-indexed 세션이라 `FindByIndexNameSessionRepository` 빈이 없고 주입하면 기동이 실패한다(`PasswordResetService` javadoc). "비밀번호를 바꿔도 기존 세션은 살아있다"가 **알려진 한계**이며 전 기기 세션 무효화는 후속(#1318). `contract.md` 비밀번호 재설정 절도 동일하게 "범위 제외"로 명시
  - > 검증 근거: `PasswordResetService`·`RedisPasswordResetTokenStore`·`AccountRecoveryService` 및 단위테스트 UT-012~021

### FR-2 촬영 데이터 업로드

- 이미지(JPG/PNG) 다중 업로드. **영상(MP4) 프레임 자동 추출은 현재 미구현 — 이번 범위는 이미지 전용**(v0.5 실측: 프론트는 MP4 선택 UI만 있고 "프레임 추출 예정" 표시, 백엔드 `allowedContentTypes`는 `image/jpeg`·`image/png`만 허용). 영상 프레임 추출(FastAPI OpenCV/ffmpeg)은 **후속 PR 범위**
- **업로드 방식(정정)**: PRD 초안의 "청크(분할·재개) 업로드"는 미채택 — 실제로는 **단일 `multipart/form-data` 요청 + `onUploadProgress` 진행률 표시**로 구현(v0.5 실측). "진행률 표시"는 유지, "청크"는 문구에서 제외. 업로드 완료 시 썸네일 생성
- 메타데이터 추출(EXIF: 촬영시각, GPS 좌표) — 추출은 구현됨. 단, 지도 뷰 좌표는 EXIF가 아닌 시설물 주소 Geocoding으로 확정(FR-4·§4·§11)
- **업로드 보안 검증**: 확장자 외 **매직바이트(파일 시그니처) 검증** + 용량 상한, 원본은 직접 서빙하지 않고 서버 측 재인코딩본(썸네일·뷰어용)만 제공

### FR-3 AI 하자 탐지

- 탐지 클래스: **균열, 박리·박락, 철근 노출 3종 확정**(2026-07-13 데이터셋 라벨링 기준). 누수·백태, 도장 손상은 데이터 확보 상황상 이번 범위에서 제외 — 필요 시 후속 확장
- 모델(**v1.0 정정 — 단일 모델 → 2모델 하이브리드**, **v1.1: 균열 모델 파인튜닝판으로 교체**): 초안은 "YOLOv8-seg 단일 모델이 균열은 마스크, 면적형은 박스로 커버"였으나, 균열 성능 개선을 위해 **균열만 U-Net(시맨틱 세그멘테이션)으로 교체**했다(PR #973). **박리박락·철근노출은 YOLOv8-seg 유지**. **v1.1: 균열 모델을 현장사진 70장 파인튜닝판 `crack_unet_resnet34_v2`로 교체**(#998) — 현장 마스크 품질 GOOD 판정 0% → **58.8%**(실측 34장), AI Hub 원 성능(픽셀 F1) 하락은 -2.6%p로 망각 시험 허용선(-5%p) 내 PASS.
  - **균열 등급 산정 단위도 함께 변경**: "이미지당 최대 인스턴스 1개" → **"이미지 전체(레터박스 패딩 제외 콘텐츠 영역) 마스크 합"**. U-Net은 인스턴스 분리를 하지 않으므로 면적 총합이 자연스러운 단위
  - ⚠️ **모델·전처리·집계는 등급 임계값과 한 묶음** — 셋 중 하나만 바뀌면 임계값 재측정이 필요하다(아래 등급 항목 참조). 이 결합 관계가 문서화돼 있지 않아 v1 임계값이 모델 교체 후에도 계속 쓰인 사고가 실제로 있었다
  - **균열 폭·길이 px 측정값 구현**(v1.1 — P1 "정량 추정"의 픽셀 단계 완료): `area_px`·`length_px`·`width_px`를 원본 해상도로 환산해 제공한다(**보고서 표기용 — 등급 산정 입력이 아님**). 폭은 `2×면적/둘레` 근사(skeletonize 대비 r=0.9989), 면적은 `mask.sum()`(`contourArea`는 -18.8% 과소평가). ⚠️ **mm 환산은 미해결** — 사진 속 기준물 없이는 불가(640 입력에서 1px≈1.5mm, 법적 기준 0.3mm는 0.2px 서브픽셀). 실측 보정용 촬영 규약은 `docs/design/crack-measurement-protocol.md`(§11)
  - **박리 내부 균열 오탐 필터**(v1.1): 균열 bbox가 박리 bbox에 80% 이상 포함되면 오탐으로 제거(실사진 10장에서 오탐 4건 제거·진짜 균열 7건 유지). 박리가 작게 잡히는 사진엔 무효 — **의도된 보수적 한정**
- **데이터셋: AI Hub「건물 균열 탐지 이미지」등 약 161,000장 확보 완료**(Colab 학습용 Google Drive 업로드 완료 — `haja19_dataset`). 학습은 Colab 등 GPU 환경에서 수행 후 서버에는 추론만 배포
- **ARM CPU 추론 최적화(설계 목표 — 현재 미적용, v1.0 갱신)**: 목표는 ONNX 변환(+INT8 양자화) + ONNX Runtime 서빙(순정 PyTorch 대비 2~4배). **실제 현재 서빙은 raw PyTorch 직접 추론** — 균열은 U-Net(`unet_client.py`), 그 외는 `ultralytics.YOLO(.pt)`(`yolo_client.py`). 인터페이스는 교체 가능하게 분리해뒀으나 최적화 파이프라인은 **후속 과제**. **v1.0 전제 변동**: 모델이 2종(U-Net + YOLO)으로 늘어 ONNX 전환 대상·검증 범위도 각각 필요해졌다(100장 10분 달성 여부 미측정, §11 오픈이슈)
- **모델 버전 관리**: 파일명 `{날짜}_{데이터셋버전}_{mAP}` 규칙 + `models/MODEL_CARD.md`(학습 데이터·하이퍼파라미터·성능·한계) 유지. **현재 모델은 HF Hub(`50seok/hajacheck-defect-detection`) 체크포인트로 실추론 동작 중이나 MODEL_CARD 버전 이력 테이블이 placeholder 상태**(v0.5 실측) — "배포 중 모델 추적" 취지가 깨져 있어 첫 행 기입 필요(후속). MLflow 실험 추적(`ai-dl/tracking.py`)의 run 이름 규칙과 일치. **v1.0**: 균열 U-Net 교체로 클래스별 모델이 갈렸으므로 MODEL_CARD도 **클래스별 체크포인트·측정조건(전처리·집계 단위)을 함께 기록**해야 한다(임계값 재측정 트리거 판단 근거)
- Spring Boot → FastAPI 비동기 추론 요청, 결과(클래스, 좌표, confidence)를 PostgreSQL 저장
- 심각도 등급: 규칙 기반(크기·유형)으로 **시설물 안전등급 A~E 5등급** 산정(§11 확정) → 점검자 수동 조정. 등급 점수 `g∈[0,1]` 0.2 단위 버킷(A=우수 0.8~1.0 … E=불량 0~0.2), 산정 위치·임계값 상세는 **`conventions/하자_심각도_등급_규칙.md`**(HAJA-109 산출물)
- **균열 등급 임계값 재캘리브레이션(v1.0 v2 → v1.1 현행 v4)**: 밴드 이력 정본은 `ai/core/grading.py` 모듈 docstring("균열 구간표 캘리브레이션 이력").
  - **v2(2026-07-27)**: U-Net 교체 + 집계 단위 변경이 둘 다 면적비를 키우는 방향이라, v1 밴드를 그대로 쓰면 **정상 범주가 대량으로 심각(D·E)으로 과대경고**됐다(보통 정답 150장 중 83장, 심각알람 정밀도 64.7%까지 하락). AI Hub 아파트 균열 470장을 현행 조건으로 재측정해 재보정(공식은 v1과 동일한 로그 등간격, 측정 중앙값만 갱신 — 역산 시 v1이 재현되어 공식 자체는 검증됨)
  - **v3(2026-07-28) — 2지표 min 합의**: 면적비 단독으로는 **"가늘지만 긴" 실금이 D·E로 과대판정**된다(U-Net 마스크 폭이 3~4px 하한에 갇혀 area가 사실상 길이만 재기 때문). **BLACKHAT 어두움총량(`dark_ratio`)**을 두 번째 지표로 추가하고 **두 지표 등급 중 낮은 쪽을 채택**한다 — 심각 판정에 "넓게 퍼졌다"와 "짙다"를 **둘 다** 요구. 실금은 어두움이 낮아, 어두운 소형 오탐은 면적이 낮아 각각 걸러진다(심각알람 F1 81.3% → 84.2%)
  - **v4(2026-08-03, 현행) — 파인튜닝 모델 기준 재교정**: 균열 모델을 `crack_unet_resnet34_v2`로 교체(#998)하자 마스크가 더 타이트해져 면적비 분포 자체가 달라졌다 → area·dark **양쪽 밴드 재교정**(분위수 그리드 + 좌표하강, 목적함수 = 심각알람 F1 우선 → 3등급 정확도). **심각알람 F1 87.9%**, **3등급 정확도 70.2% → 79.6%**
  - **재측정 트리거 규칙**: 임계값은 "모델 · 전처리 · 집계 단위" 3조건에 종속된다 — 셋 중 하나라도 바뀌면 재측정한다. v1 사고의 근본 원인이 "임계값 옆에 측정 조건이 적혀 있지 않았던 것"이므로, 임계값과 측정조건은 항상 같은 자리에 기록한다. **v4가 이 규칙이 실제로 발동한 사례**(모델 교체 → 즉시 재교정). `dark_ratio` 산출 조건(BLACKHAT ellipse 15×15 등)이 바뀌어도 dark 구간표 재측정 대상이다
  - ⚠️ **프레이밍(촬영거리) 의존성은 미해결**(v1.1 명시): area_ratio는 "촬영 조건이 유사하다"는 전제 하의 추정치다 — 같은 균열도 2배 가까이서 찍으면 값이 1.5~1.8배로 변한다. **"촬영거리로 보정" 안은 2026-07-27 실측으로 철회**됐다(폭·거리 상관 ρ≈-0.26로 너무 약해 과보정 → AUC 0.87→0.78, 게다가 촬영거리가 심각도와 교란). 밴드를 넓히면 불량이 다시 A·B로 몰려 원래 버그로 회귀하므로 밴드 조정으로도 대응하지 않는다 — **현재 미해결**(§11)
  - 임계값은 여전히 상수 모듈 하드코딩 — DB화는 후속(옛 "하자 유형·등급 기준 관리" 화면이 메뉴에서 삭제됐으므로 DB화 시점은 재검토 대상, §11)

### FR-4 결과 시각화·검수

- 이미지 뷰어에 탐지 결과 오버레이, confidence 임계값 슬라이더
- 점검자가 오탐 삭제/누락 추가/등급 수정 가능(휴먼 인 더 루프)
- **오탐 삭제 되살리기(v1.1 신규)**: 삭제는 Soft Delete이므로 **되살리기 + 삭제 사유 조회**를 제공한다(#1400) — 오탐 판정을 잘못한 경우의 복구 경로. 단, **재분석으로 대체된 분(이전 회차 산출물)은 되살리기 대상에서 제외**한다(#1402) — 되살리면 현행 분석 결과와 중복·모순이 생긴다
- **등급·bbox 결측 하자 처리(v1.1)**: ①등급 없는 신규 하자가 **등급 없이 검수 확정돼 영구 미분류로 고착**되던 문제 차단(#1398) ②등급·bbox 없는 하자를 결과 뷰어에서 **선택조차 못 해 점검 요약이 영구 잠기던** 문제 해소(#1396)
- **점검(Inspection) 상태머신 6단계(v1.1 명문화)**: `CREATED → UPLOADING → ANALYZING → ANALYZED → REVIEWED → REPORTED` — 하자 상태머신(아래)과 **별개 축**이다(하자=개별 결함의 조치 진행, 점검=회차 산출물의 진행). 전이 정본은 `core/inspection/entity/InspectionStatus.java`
  - `ANALYZING → ANALYZED`(성공) / **직전 상태로 롤백**(전체실패·큐포화·고착 복구·리퍼 복원). 선점은 원자적 조건부 UPDATE(`startAnalyzingIfNotRunning`)
  - **`ANALYZED → REVIEWED`는 점검 요약 진입 시 전이**(v1.1 배선 — #1429. 검수 확정 시점을 보고서 생성 직전이 아니라 요약 진입으로 **앞당김**)
  - **`REVIEWED → REPORTED`는 보고서 확정 시 전이**(v1.1 배선 완료 — #1381). `REPORTED`는 종단
- **수정 이력 구조화**: `defects` 직접 UPDATE 대신 `defect_revisions` 테이블에 append-only 기록(누가·언제·무엇을 변경) — 검수의 감사 추적 보장, 삭제는 Soft Delete
- **하자 상태머신(4단계)**: `신규(AI탐지) → 검수확정 → 조치중 → 조치완료` 표준 전이만 허용(역방향·건너뛰기는 사유 기록 필수). 상태 전이 API = `PATCH /api/defects/{id}/status`
  - **`조치대기`(`ACTION_PENDING`)는 V22에서 제거**됐다(#1072/#1083/#1097, `V22__collapse_defect_status_action_pending.sql`). "검수는 끝났고 조치는 아직"이라는 상태를 `검수확정`(`CONFIRMED`)이 그대로 겸하므로 단계를 따로 둘 실익이 없었고, 실사용에서도 두 상태의 구분 기준이 사람마다 달랐다. 기존 `ACTION_PENDING` 행은 마이그레이션에서 `CONFIRMED`로 백필한다.
  - enum 정본은 `core/defect/entity/DefectStatus.java` = `DETECTED · CONFIRMED · IN_PROGRESS · RESOLVED`
- **하자 관리 화면 재해석(v0.5, 2026-07-24 확정)**: 하자 단건 목록이 아니라 **점검(Inspection) 단위 그룹 목록**(`GET /api/inspections` — 페이지네이션, 로우 = 점검일·시설물·하자건수·등급분포) → **점검 상세(카드형 하자 그리드 + KPI 4종 헤더) → 하자 상세 풀스크린 모달** 2단계 진입
- **조치 결과 등록(v0.5 신규, v1.2 정정 — `targetStatus` 도입 #1128)**: 조치후사진·조치내용·조치일·담당자 4필드 **+ `targetStatus`(진행상태 select) = 총 5필드 필수** 폼 → `PATCH /api/defects/{id}/action`. 담당자 드롭다운은 `GET /api/facilities/assignable-users` 재사용(회사 소속·cross-company IDOR 검증)
  - **"`/status`와 분리"의 정확한 의미**: 분리된 것은 **엔드포인트와 계약**이지 상태 전이 자체가 아니다. `/action`은 `targetStatus`로 **상태를 함께 전이**하며, 허용 값은 `IN_PROGRESS`·`RESOLVED` 2개뿐이다(`DETECTED`·`CONFIRMED`는 400). 전이 가능 여부는 `Defect#changeStatus`의 정방향 1단계 규칙을 그대로 따라 `CONFIRMED→IN_PROGRESS`·`IN_PROGRESS→RESOLVED`만 사유 없이 허용되고, 건너뛴 전이는 거부된다(폼에 사유 입력란이 없기 때문). 전이 성공 시 `defect_revisions`에 상태 변경 이력이 남는다
  - v1.1까지의 "상태 전이와 의도적으로 분리"라는 표현은 "`/action`은 상태를 바꾸지 않는다"로 읽힐 수 있어 정정한다(계약 정본 = `openapi.yaml` `/api/defects/{id}/action`)
- **조치 등록 이력 append-only(v1.0 신규)**: `defects`의 조치 4필드는 **"최신 스냅샷"만 담는 단일 값 컬럼**이라, 조치중(`IN_PROGRESS`) 단계에서 시간차를 두고 여러 번 등록하는 사진·조치내용이 **매번 덮어써지고 이력이 남지 않았다**. `defect_revisions`는 필드 변경 감사용(구/신 값이 짧은 문자열)이라 사진 URL을 담기에 스키마가 맞지 않아, **조치 등록 제출 자체를 통째로 append하는 `defect_action_logs` 테이블을 신설**했다(`phase` = `IN_PROGRESS`/`RESOLVED` 2단계 구분)
- **활동 기록 타임라인(v0.5)**: 감사용 `defect_revisions`를 화면에 노출 — 상태 변경·담당자 배정·조치 내용을 사용자용 타임라인 UI로 표시(v1.0: 조치 등록분은 `defect_action_logs` 기반으로 다회 이력까지 표시 가능)

### FR-5 LLM 보고서 생성 (LangChain)

- LLM: **Qwen3-8B (Apache 2.0) — HuggingFace Serverless Inference API** 호출 (종량제, HF PRO 구독)
- LangChain 체인 구성: 시설물 정보 + 확정 하자 목록·통계 → 보고서 생성 프롬프트 → LLM
- **템플릿 채우기 방식**: 자유 서술이 아닌 섹션별 짧은 생성 → 조립 구조로 sLLM 품질 편차 방어
- RAG: 점검 지침·법규 문서(Chroma Retriever) 근거 인용
- **Grounding Check(사실 검증)**: 생성 체인 마지막에 후처리 검증 단계 — LLM이 언급한 하자 개수·등급을 `defects` 실측치와 코드로 대조, 불일치 시 해당 섹션 재생성 또는 편집 가능한 초안에 경고 배지 표시(sLLM 수치 환각 방어). PDF 확정은 검증 `PASS` 결과만 허용하며, `WARN` 결과는 수정·재생성 후 재검증한다.
- **섹션 병렬 생성**: 독립 섹션(개요/요약/상세/권고)은 `RunnableParallel`로 동시 호출 — 순차 대비 지연시간 단축(100장 10분 목표 대응)
- **LangGraph StateGraph 오케스트레이션(v0.5 도입, #703)**: grounding 결과에 따른 섹션 재생성(재시도)을 손으로 짠 `while` 루프 대신 **노드 + 조건부 엣지(그래프 cycle)**로 재구성. `detail_validation ⇄ detail_retry`·`grounding_check ⇄ summary_retry` 2개 사이클, 최대 2회 재시도(`recursion_limit=20` 명시), 소진 시 기존 동작과 100% 동일(detail은 `ValueError`, summary는 `grounding_ok=False`로 진행). 재시도가 LangSmith 트레이스·`draw_mermaid()`에 개별 단계로 드러남. `get_llm()`/`check_grounding()`는 노드 본문에서 런타임 호출(모듈 캐싱 금지 — 테스트 `@patch` 호환), checkpointer 미사용(무상태 단발)
- 출력: 점검 개요, 하자 현황 요약표, 유형별 상세, 조치 권고, 종합 의견 → 편집 후 PDF 내보내기
- **관공서 표준양식 PDF(v1.1 신규, 1차 적용 — #1294)**: 자유 레이아웃이 아니라 **관공서 제출 양식에 맞춘 서식**으로 렌더한다. 함께 반영된 항목:
  - **책임기술자 표기** + 결과 요약과 최종 확정 검증의 정합(#1409)
  - **하자 탐지 박스 좌표 배선** — 화면 미리보기와 PDF가 **같은 좌표 소스**를 쓰도록 통일하고 사진 중복 제거(#1333). 이전에는 PDF에 박스가 표시되지 않았다
  - 보고서 **저장/확정검증 통합** + 임시저장·미리보기 가드 개선(#1341)
  - ⚠️ **잔여**: 정밀안전점검 표준서식의 서두(제출문·참여기술진 명단)·자료수집 및 분석·안전등급 지정·부록은 **아직 미대응**(§11-9)
- **생성 파이프라인 방어로직(v0.5 실측 — 구현이 PRD 초안보다 강함)**:
  - **프롬프트 인젝션 방어**: 사용자 입력에 섞인 악성 지시문이 시스템 프롬프트로 오인되지 않게 격리
  - **인용(법 조문) 검증**: LLM이 실제 참조하지 않은 조문을 인용한 것처럼 쓰지 못하게 코드로만 근거 검증(환각 인용 차단)
  - **백엔드 프록시**: 프론트가 AI 서버를 직접 호출하지 않고 백엔드가 대행 — 인증·에러코드 통일
  - **이중 Rate Limit**(사용자 단위 + 전체 단위) + **백엔드↔AI 서버 내부 인증 헤더**(별도 키)
  - **타임아웃 세분화**: 연결 실패와 응답 지연을 구분 처리
  - **RAG 폴백 견고성**: RAG 검색이 인프라 문제로 실패했을 때만 조용히 폴백, 코드 버그면 그대로 표면화(숨김 금지)
  - **LLM 클라이언트 재시도 + 캐시 스키마 불일치 시 자동 삭제**
- **보고서 저장·확정(v0.5 실측)**: `reports`는 **DRAFT/FINALIZED 상태 관리** + 같은 점검에 다중 버전 방지 **DB 유니크 제약** + **JPA 낙관적 락**. PDF는 **매직넘버 검증**, Grounding 미통과 보고서는 **확정 차단**, PDF 다운로드 URL **소유권 재검증**, 저장 경로 **path traversal 차단**, AI 응답 대기 중 **DB 커넥션 미점유**(커넥션 풀 고갈 방지)

### FR-6 RAG 챗봇 (LangChain)

- 시설물안전법·안전점검 세부지침 등 문서 임베딩(Chroma), LangChain RetrievalQA 파이프라인
- **하이브리드 검색 — 벡터 + BM25(RRF 결합)**(v1.1 신규, #1410/#1420 — `regulations` 컬렉션): 법조문은 **정확한 용어·조번호 매칭**이 중요해 순수 벡터 유사도만으로는 "제12조" 같은 키워드 질의에 약하다. 벡터검색과 BM25 키워드검색을 각각 수행해 **Reciprocal Rank Fusion**으로 결합한다.
  - **RRF 상수는 관용값 60이 아니라 `k=1`로 튜닝**(실측: law.go.kr 공식 법규 PDF 7건 · 질의 27개). k=60은 `1/(60+rank)` 항의 차이가 너무 작아 순위 정보가 사실상 반영되지 않았다 — k=1에서 recall@10·MRR·nDCG 전부 개선(recall@4만 근소 하락, 벡터 top-4가 놓친 질의를 BM25가 복구하며 생기는 트레이드오프)
  - 반환 형태는 `similarity_search()`와 동일한 `list[Document]`로 유지 — 기존 출처·컨텍스트 조립부는 무수정
  - 문서 재적재(ingest) 시 **BM25 캐시 무효화 훅** 연결
- **검색 품질 평가 하네스**(v1.1 신규, `ai/eval/`): 벡터-only vs 하이브리드를 **recall / MRR / nDCG**로 정량 비교(`run_eval.py`, CSV·PNG 산출). 지표 순수함수는 단위테스트 대상이고, 실행 자체는 로컬 전용(pytest 대상 아님)
- 점검 기준·법규 질의 **전담** (데이터 질의는 하자 관리 "자연어 검색"으로 일원화 — 기능 중복 방지), 답변 출처 표시
- ~~대화 이력 관리(LangChain Memory + Redis)~~ — **v1.2: 미구현(후속 범위)**. 각 질의는 **무상태 단발**로 처리된다. FastAPI 요청 스키마에 `session_id`(선택) 자리만 잡혀 있고 체인은 이 값을 쓰지 않으며, 백엔드 프록시(`RagChatAiRequest`)는 아예 전송하지 않는다. 아래 LangGraph 항목의 "멀티턴·재검색 루프는 후속 범위"와 같은 맥락이며, v1.1까지 두 문장이 서로 모순돼 있던 것을 정정한다
- **LLM 응답 캐시**: 프롬프트 해시 키 기반 Redis 캐시 — 개발·테스트 중 동일 질의 반복 호출로 인한 HF 크레딧 소진 방지
- **LangGraph StateGraph 오케스트레이션(v0.5 도입, #710)**: 단발 파이프라인(`cache_check → retrieve → answer → build_sources → cache_write`)을 그래프로 감싸고 조건부 엣지 2곳 적용 — 캐시 히트 시 즉시 종료, 검색 0건이면 `no_result`(캐시 저장 생략, `RAG_NO_RESULT` 응답). 멀티턴·재검색 루프는 후속 범위로 미포함(YAGNI)

### FR-7 상담 챗봇 & 상담원 연결

- **시나리오 챗봇**: 버튼 기반 플로우(예: 서비스 이용 방법 / 분석 결과 문의 / 요금·기타) — 시나리오는 관리자 페이지에서 등록·수정. 진입은 **전역 플로팅 버튼(우하단)**, 고객지원 메뉴는 이력 조회 중심
- **상담원 연결**: 시나리오에서 해결 안 될 경우 "상담원 연결" → 상담 대기열 등록 → 상담원과 **실시간 1:1 채팅(WebSocket/STOMP)**
- **상담원 전용 콘솔 분리(v1.0 신규)**: v0.5까지는 상담원 채팅 콘솔을 플랫폼 관리자 콘솔 안의 메뉴로 뒀으나, 실제 구현은 **`/counsel-console` 독립 셸 + 전용 로그인(`/counsel-console/login`)**이다 — `COUNSELOR` 역할 전용이며 일반 사용자 셸·플랫폼 관리자 셸과 분리된다(상담원은 점검·시설물 기능에 접근하지 않음).
  - 구성: 상담 대기열 목록 → 티켓 선택 → 1:1 채팅 창 + 고객 정보 패널
  - **상담원 스킬**(이용 방법 / 분석 결과 / 결제·기타) 축으로 배정 — 스킬 부여·변경은 플랫폼 관리자 사용자 관리에서 수행
  - **플랫폼 관리자 쪽 "상담 관리"는 역할이 바뀐다** — 실시간 응대가 아니라 **날짜별 상담 티켓 조회 + 읽기전용 대화 열람**(운영 모니터링·품질 확인용)
- 상담 종료 시 대화 내역 저장, 고객은 고객지원 > 내 상담 내역에서 조회 가능
- 상담원 부재 시: 대기 순번 안내 + 문의 남기기(오프라인 티켓)
- **WebSocket 인증**: STOMP `CONNECT` 프레임에서 `ChannelInterceptor`로 Spring Session 검증 — 세션 없는 연결 즉시 거부(HTTP 인증 미들웨어가 WS에 자동 적용되지 않는 누락 지점)

### FR-8 관리자 페이지 (v1.0 개정 — 기업 관리자 / 플랫폼 관리자 2개 콘솔 + 상담원 콘솔은 FR-7로 분리)

**공통 원칙**: `users.company_id`를 기준으로 데이터 스코핑 — 기업 관리자 콘솔의 모든 조회·변경 쿼리는 `WHERE company_id = :myCompanyId` 강제 필터를 백엔드에서 병행(프론트 숨김만으로 대체 불가, FR-1 권한 집행 원칙과 동일). 플랫폼 관리자는 `company_id` 필터 없이 전체 조회.

**FR-8-A 기업 관리자 콘솔** (기존 관리자 경로, 자기 `company_id` 범위 — v0.5: 2개 메뉴로 축소)

- 사용자 관리: 자기 기업 소속 회원 목록·검색, 권한 변경, 상태 변경(활성/정지)
- 플랜·쿼터 관리: 내(자기 회사) 플랜 변경, 사용량(월 분석 장수) 조회 (§2.4)

**FR-8-B 플랫폼 관리자 콘솔** (`/platform-admin` 별도 로그인, 전체 기업 대상 — `PLATFORM_ADMIN` 외 로그인 거부. **v1.0: 메뉴 재구성 — 대시보드 최상위 개칭, 하자 유형·등급 관리 삭제, 상담 관리 역할 축소**)

- **대시보드(v1.0 — 옛 "서비스 통계" 개칭·최상위 배치)**: 가입자 수(전체 기업)·분석 요청 수·상담 건수 등 전사 지표. **콘솔 기본 진입점** — 로고 클릭·인덱스(`/platform-admin`) 진입 모두 이 화면
- 사용자 관리: 전체 기업/회원 목록·검색, 권한 변경, 상태 변경(활성/정지), **상담원 스킬 부여·변경**(v1.0)
- **플랜·쿼터 관리(P1)**: 기업(사용자)별 플랜 조회·변경, 사용량(월 분석 장수) 모니터링 + **사업자등록번호 인증 상태 확인**(§2.4 결제 진입 게이트 — v1.1: 배지는 **provenance 기준**, FR-1) + **결제 이력·예약 하향 현황**(v1.0)
  - **KPI 산출 기준 정정(v1.1 — #1413)**: 플랜·쿼터 KPI와 서비스 통계의 사용량을 `media` 테이블 **근사치**로 세던 것을 **`usage_counters` 실사용량**으로 교체 — 쿼터 차감의 정본과 화면 표시가 어긋나던 문제
- **상담 관리(v1.0 재정의)**: **날짜별 상담 티켓 조회 + 읽기전용 대화 열람**(운영 모니터링·품질 확인) + 챗봇 시나리오 CRUD. **실시간 응대(대기열·채팅)는 상담원 전용 콘솔로 분리**(FR-7)
- RAG 문서 관리: 법규·지침 PDF 업로드 → 임베딩 파이프라인 실행 — **재임베딩은 명시적 배치 잡으로 분리**(Chroma 임베디드 쓰기 락이 서비스 중 챗봇 질의를 막지 않도록)
  - **문서 삭제 기능**(v1.1 신규 — #1411): 업로드된 RAG 문서의 삭제(문서 + 임베딩 정리)
  - **임베딩 504·거짓 상태 수정**(v1.1 — #1393): 임베딩이 실제로는 진행 중인데 타임아웃으로 실패 표시되거나, 실패했는데 완료로 표시되던 문제를 상태 필드 추가로 해소(`embedding_started_at`·`expected_embed_batch` — V39/V40)
  - **임베딩 모델 warmup**(v1.1 — #1295): `BAAI/bge-m3` 콜드스타트로 첫 질의가 지연·실패하던 문제 방지
- **시스템 모니터링** (간단 대시보드):
  - 서비스 헬스체크: Spring Boot Actuator(`/actuator/health`) + FastAPI(`/health`) 상태를 주기 폴링, 정상/이상 상태 표시
  - 분석 잡 큐 현황: 대기/진행/완료/실패 건수 (Redis 잡 상태 집계), 실패 잡 재실행 버튼 — **v1.1 실데이터 구현 완료**(#1414, 이전에는 목/플레이스홀더)
  - 최근 에러 로그: API 5xx·분석 실패 최근 N건 조회
  - 서버 자원: CPU·메모리·디스크 사용률 (Actuator metrics 활용)
  - ~~HF Inference API 호출량·누적 토큰 카운트 (예산 관리용)~~ — **v1.2: 미구현·대체 확정**. HF는 사용량 조회 공개 API가 없어 이 카드를 만들 수 없다 → **서버 자원(CPU·메모리·디스크) 카드로 대체**(#728, `monitoring.types.ts` 주석에 근거 기록). 사용량 추적이 필요하면 자체 호출 카운터를 두는 별도 과업으로 분리
  - ~~**예산 가드레일**: 일일/주간 토큰 한도 초과 시 관리자 경고 배지 + 초과 심화 시 디그레이드 모드~~ — **v1.2: 미구현(후속 범위)**. 위 사용량 카운터가 선행 조건이라 함께 보류된다. 현재 LLM 비용 방어는 **이중 Rate Limit(사용자·전체)과 Redis 응답 캐시**가 담당한다(FR-5)
  - > ⚠️ 현재 `GET /api/platform-admin/monitoring` 응답은 **`serverHealth`·`jobQueue`·`resourceUsage`·`errorLogs` 4종**이다(`SystemMonitoringResponse`). 위 2개 항목은 응답에 없다
- ~~하자 유형·등급 기준 관리(코드 테이블)~~ — **v1.0: 메뉴·라우트에서 삭제**(v0.5의 "향후 개발 이관"을 실제 제거로 확정. 등급 임계값은 현재 상수 모듈 하드코딩이며 DB화 시점은 재검토 — FR-3·§11)

### FR-9 알림 센터 (P1)

- 헤더 벨 아이콘 + 알림 드롭다운/목록, 미읽음 뱃지, 읽음 처리
- 알림 이벤트: 분석 완료, 검수 대기 발생, 상담 답변 도착, 점검일 도래(점검 주기 연계) + **구독 전이 3종(v1.0)**: 플랜 만료 · 하향 완료 · **하향 실패**(예약 하향 처리 중 오류 — 운영자 개입 필요 신호)
- 구현: `notifications` 테이블 + **클라이언트 조회(마운트·창 포커스 시 refetch)** — WebSocket 푸시·이메일은 범위 제외(상담용 WS와 별개로 단순 유지)
  - **v1.2 정정**: v1.1까지 "폴링(30초)"으로 기술했으나 **주기 폴링은 구현되지 않았다** — `useNotifications`는 `refetchInterval` 없이 react-query 기본 동작(마운트·포커스 복귀 시 갱신)만 쓴다. 화면을 열어둔 채로는 새 알림이 즉시 뜨지 않는다는 뜻이며, 주기 폴링 도입 여부는 후속 결정 사항
- 이벤트 발행은 각 도메인 서비스에서 공통 `NotificationService` 호출로 통일
- **발행 배선 완료(v1.1)**: AI 분석 완료 시 `ANALYSIS_DONE`·`REVIEW_PENDING`을 **실제로 발행**한다(#1283 — 이전에는 타입만 정의돼 있고 발행부가 없어 알림이 뜨지 않았다). 알림 목록의 "결과 보기 / 검수하기" 버튼도 해당 화면으로 **네비게이션 연결**(#1288)

---

## 6. 시스템 아키텍처

**아키텍처 결정: 모놀리식** (Spring Boot 단일 앱 + FastAPI). JEUS 미사용 — Nginx가 Spring Boot 내장 Tomcat으로 직접 프록시.

```
[React SPA (Axios, WebSocket)]
      │ HTTPS / WSS
[Nginx (80/443)]
  ├─ React 정적 빌드 서빙 (/)
  ├─ /api/** ─▶ Spring Boot:8080 (리버스 프록시)
  └─ /ws/**  ─▶ Spring Boot:8080 (WebSocket 업그레이드 프록시)
      │
[Spring Boot 단일 앱 (JDK17, 내장 Tomcat, 8080)]
  ├─ 인증: OAuth2 (Kakao/Google) + Spring Session ─ Redis
  ├─ 실시간 상담: WebSocket/STOMP (상담원 연결)
  ├─ 업무 데이터 ─ PostgreSQL (사용자, 시설물, 점검, 하자, 보고서, 상담, 결제)
  │    · 스키마 변경 = Flyway forward migration (V1~V40, ddl-auto=validate)
  ├─ 결제 ─▶ [토스페이먼츠 API] (샌드박스 — 주문/승인, §2.4)
  └─ 추론/생성 요청 ─▶ [FastAPI (Python, 8000 — 내부 네트워크 전용)]
                        ├─ DL 모델 (하자 탐지 — 클래스별 2모델)
                        │    · 균열: U-Net(`crack_unet_resnet34_v2`, 현장사진 파인튜닝) 시맨틱 세그멘테이션
                        │    · 박리박락·철근노출: YOLOv8-seg
                        ├─ OCR (사업자등록증 — rapidocr)
                        ├─ LangChain + LangGraph (보고서 생성·RAG 챗봇 = StateGraph 오케스트레이션)
                        │    · LangSmith 트레이싱(관측) — get_llm() 자동 계측, env 주입
                        ├─ LLM: Qwen3-8B — HuggingFace Serverless Inference API
                        │    (종량제, HF PRO 구독)
                        └─ Chroma VectorDB — FastAPI 내 임베디드 모드(로컬 파일 저장)
                             임베딩: HuggingFace 한국어 모델(ko-sbert / BGE-m3, CPU — 무료)
[파일 스토리지] 업로드 원본·썸네일·오버레이 이미지
```

- **모듈러 모놀리스**: Spring Boot 내부 패키지를 도메인별(auth / core / counsel / admin)로 엄격 분리 — 향후 서비스 분리 가능한 구조로 설계
- **ORM: Spring Data JPA(Hibernate)** — Entity·Repository 규약은 `SpringBoot_코드_컨벤션.md` 참조. 프론트 규약은 `React_코드_컨벤션.md` 참조
- **이메일 발송: Spring Boot Starter Mail(SMTP)**(v0.45 신규, **v1.2 정정 — 용도 축소**) — **비밀번호 재설정 링크 발송 전용**(인증코드 발송 용도는 미구현, FR-1 참조). 실제 발송은 비동기 처리(`@Async`)로 API 응답 지연 방지
- **프론트엔드 공통 UI 스택**(v0.44): 스타일링은 **Tailwind CSS 4.3.2**로 통일, 대시보드·통계·회차 비교 등 화면 전반의 차트·그래프는 **Recharts 3.9.2**를 공통 컴포넌트로 사용(개별 화면별 차트 라이브러리 혼용 금지)
- Spring Boot ↔ FastAPI: REST + 비동기 잡 패턴(분석 요청 → 잡 ID → 폴링/콜백). FastAPI·DB·Redis·Chroma는 외부 포트 미개방(내부 통신 전용)
- **FastAPI 워커 분리(설계 목표 — 현재 미구성, v0.5 정정)**: 목표는 CPU-bound YOLO 추론을 별도 워커 프로세스(`ProcessPoolExecutor` 또는 uvicorn `--workers`)로 오프로드(3 OCPU: 탐지용 1 + API용 1~2). **실제 배포는 `uvicorn main:app` 단일 프로세스**(`docker-compose.arm1.yml`/`override.yml`에 `--workers`·`ProcessPoolExecutor` 없음). `detect_defects`가 동기 `def`라 FastAPI 기본 스레드풀로 돌지만 프로세스 단위 격리는 **후속 과제**
- **Correlation ID**: 모든 분석 잡·요청에 `X-Request-Id` 발급, Spring·FastAPI 양쪽 로그에 동일 ID 기록 — 경계 넘는 요청 추적(디버깅 시간 절감)
- 영상 프레임 추출: FastAPI 측 OpenCV/ffmpeg — **미구현, 후속 범위**(현재 업로드는 이미지 전용, FR-2)
- **Redis 활용처**(v1.2 정정 — ③·⑧): ① Spring Session(로그인 세션) ② 분석 잡 상태·진행률 캐시 ③ ~~LangChain 챗봇 대화 메모리~~ **미구현(FR-6)** ④ 상담 대기열(Sorted Set) ⑤ 대시보드 통계 캐시 ⑥ OAuth 인가 state 임시 저장 ⑦ LLM 응답 캐시(프롬프트 해시 키) ⑧ ~~이메일 인증 코드(`email:verify:*`)~~ → **비밀번호 재설정 토큰**(`sha256(token)` 해시 저장, 1회성) ⑨ 초대 코드

### 6.1 배포 환경 — OCI (Oracle Cloud Infrastructure)

> **📌 실배포 현황(2026-07-16, #258·#260) — 아래 "전용 VM 단일 호스트"는 목표(prod/향후)이고, 현재 dev 배포는 공유 호스트다.**
> A1 전용 VM 확보 전까지 dev는 **oci-arm1 공유 호스트**(hajacheck 외 7개 사이트와 공존)에서 운영한다. 공유 호스트에선 80/443이 호스트당 1개뿐이라 하자첵 nginx가 이를 독점할 수 없다 → **유일한 PRD 이탈 = "엣지 80/443 TLS 종료 nginx가 공용 host nginx"** 1건. 나머지(내부 bridge 네트워크·전용 postgres/redis 컨테이너·dev/prod DB 분리·이미지 SHA 태그)는 모두 준수하며, 하자첵 전용 nginx 컨테이너는 그 뒤 `127.0.0.1:8100`에서 앱 라우팅(/api·/ai·/ws·정적)을 담당한다(`docker-compose.arm1.yml`, 2026-07-16 B안 확정).
> - **dev/prod 분리(v1.1 정정 — 분리 축은 VM이 아니라 DB다)**: v1.0의 "arm1 = dev 전용, prod는 전용 VM 확보 후 이관"은 **현행이 아니다**. **arm1 한 대에서 dev·prod를 함께 가동**하며, 분리는 **postgres 인스턴스 2개**로 한다.
>   - **dev(팀 공용)** = host postgres-16, `127.0.0.1:5432`, DB **`hajacheck_dev`** — 팀원이 SSH 터널로 공유
>   - **prod(실서비스)** = 전용 postgres 컨테이너, `127.0.0.1:5433`, DB **`hajacheck`** — arm1-spring이 붙는 실서비스
>   - 포트가 다르므로 **서로 다른 postgres 서버 프로세스**다. 터널로 dev를 띄워도 prod에는 무영향이지만, 공유 dev DB는 실제로 건드린다(§6.3 Flyway 규칙의 전제)
>   - `hajacheck_prod`라는 DB명은 **사용하지 않는다**(v1.0 서술의 "예약"은 폐기). Redis는 인덱스로 분리
> - **전용 VM 확보는 여전히 향후 목표** — 확보 시 아래 구성(nginx 포함 전 구성요소 compose, 80/443 독점)을 그대로 적용하면 PRD 이탈 0. **현재 상태가 아니라 목표다.**

**인프라: OCI Always Free — Ampere A1 VM 1대 (ARM/aarch64, 3 OCPU / 16GB RAM, Ubuntu 22.04)**

```
[도메인 DNS] ── A레코드 ──▶ [OCI Reserved Public IP(고정)]
                                    │
[OCI VCN — 퍼블릭 서브넷, Security List: 80/443/22만 개방]
 └─ Ampere A1 VM ─ Docker Compose (단일 호스트, 내부 bridge 네트워크 1개)
     ├─ nginx    :80/:443 — React dist 서빙, 리버스 프록시, certbot(TLS 종료)
     ├─ spring   (내부) — nginx가 /api·/ws 프록시
     ├─ fastapi  (내부) — nginx가 /ai 프록시, 외부 차단. YOLOv8-seg 추론(CPU) + Chroma 임베디드(볼륨)
     ├─ postgres (내부) + 127.0.0.1:5433 publish — 팀 SSH 터널 전용 (prod DB `hajacheck`)
     │    · 별도로 host postgres-16 :5432 = 팀 공용 dev DB `hajacheck_dev` (위 실배포 현황 참조)
     └─ redis    (내부) + 127.0.0.1:6380 publish — 팀 SSH 터널 전용, requirepass·AOF
   volumes: pgdata · redisdata · chroma_data · certbot 인증서
```

- **서버 구성 = Docker Compose 단일 호스트**: 전 구성요소를 compose 서비스로 관리(내부 bridge 네트워크, 외부 노출은 nginx 80/443만). ARM(aarch64) 호환은 검증됨 — 베이스 이미지 전부 multi-arch, ML 의존성(torch·onnxruntime)은 aarch64 사전빌드 휠 사용, Redis는 이미 컨테이너로 운영 중. Chroma는 임베디드 모드(볼륨 파일 저장) 유지 → 별도 프로세스 없음.
- **이미지 버전 = 레포 추종**(단일 진실): Dockerfile은 `build.gradle`(Java 17·Gradle 8.14.3)·`requirements.txt`·`package.json`(Vite 6·React 18)을 그대로 빌드하고 버전을 중복 명시하지 않는다. 이미지 태그는 `latest` 금지 — 버전+커밋 SHA 병행. 멀티스테이지·non-root `USER`·`.dockerignore`·`HEALTHCHECK`·`depends_on: condition: service_healthy` 적용.

**개발 환경 — 공용 개발 DB (SSH 터널 접속)**

- OCI 서버의 PostgreSQL·Redis **컨테이너**를 **팀 공용 개발 DB**로 사용. 포트는 공개하지 않고 **SSH 터널 전용 계정(`hajadev`, permitopen 5432·6380 제약)**으로만 접속 — `application-local.yml`에 터널 로컬 포트 설정. 컨테이너는 호스트 `127.0.0.1:5432`·`:6380`에 publish → 터널 대상 주소가 불변이라 **Docker 전환 후에도 팀원 재온보딩 불필요**. (로컬은 필요 시 동일 compose로 재현)
- **보안 필수 조치**:
  - 5432·6380은 **공개 미개방** — OCI Security List는 80/443/22만. DB/Redis는 SSH 터널로만 도달
  - PostgreSQL: 강력한 비밀번호, 컨테이너는 내부 bridge 네트워크에만 노출
  - Redis: `requirepass` 필수 설정(미설정 시 인증 없이 전체 접근 가능) + AOF
- **개발/운영 분리(v1.1 실측)**: PostgreSQL은 **`hajacheck_dev`(dev, host :5432) / `hajacheck`(prod, 전용 컨테이너 :5433)** 로 분리 — 같은 VM 위의 **별개 postgres 인스턴스 2개**다. Redis는 DB 인덱스 분리(운영 0번 / 개발 1번)
- 팀 규칙: 공용 dev DB의 스키마 변경(DDL)은 사전 공지 후 진행. **개발 DB에 실사용자 데이터 저장 금지** — 시연·테스트용 합성 데이터만 사용(개인정보가 팀원 전원의 로컬 접속 구간에 노출되는 것 방지)
- 저장소의 compose 정의는 **정식 배포 수단**(운영·로컬 공용): `docker-compose.yml`(base) + `docker-compose.override.yml`(로컬 개발, 핫리로드) + `docker-compose.prod.yml`(운영). 오프라인·파괴적 테스트도 동일 정의로 격리 실행
- 방화벽 이중 설정 주의: OCI Security List + VM 내부(ufw/iptables) — 공개는 80/443/22만
- Nginx WebSocket 프록시 설정(`Upgrade`/`Connection` 헤더) 필수 — 상담 기능 전제조건
- 보안: SSH 키 인증, 외부 노출은 nginx 80/443만, DB(5432)·Redis(6380)는 SSH 터널 전용(미개방), fastapi는 내부 네트워크 전용
- LLM(HF Serverless API)은 외부 API 호출이라 서버 사양 무관, YOLO 추론은 ARM CPU로 수행(데모 수준 충분)

**CI/CD: GitHub Actions**

- CI (PR 시): Spring Boot 빌드+테스트, FastAPI 테스트, React 빌드 — path filter로 변경 파트만 실행 (네이티브 실행, 현행 유지)
- CD (main 승격 시): VM에 SSH → `git pull && docker compose build && docker compose up -d`. **ARM 이미지는 VM에서 직접 빌드**(VM 자체가 aarch64 → 크로스빌드·외부 레지스트리 불필요), BuildKit 레이어 캐시로 재빌드 최소화. 코드는 빌드 시 이미지에 baked-in(COPY), 데이터는 볼륨에 영속. 배포 후 `HEALTHCHECK`로 기동 확인
- SSH 키·서버 IP 등은 GitHub Secrets 관리
- **필수 시크릿 — 미설정 시 배포 하드실패(v1.1)**: `BUSINESS_REGISTRATION_API_KEY`(국세청 진위확인)는 compose에서 `:-`(기본값 허용) → **`:?`(미설정 시 기동 실패)** 로 강제됐다. 이유는 **키의 보안 성질이 뒤집혔기 때문** — 가입 자동승인(FR-1) 도입 전에는 "키 미설정 → 인증 미완 → 스코프 닫힘"(fail-closed)이었으나, 도입 후에는 "키 미설정 → 진위확인 없이 자동승인 → **전면 개방**"(fail-open)이 된다. `AI_INTERNAL_KEY`·`SMTP_PASSWORD`와 **동급 전제조건**으로 취급한다

**리스크 대비**

- A1 무료 인스턴스는 리전 용량 부족("Out of capacity")이 잦음 — **착수 주간(7/9~13)에 선확보**, 실패 시 PAYG 전환(Always Free 한도 내 과금 없음)
- **시연 백업 플랜**: 완료보고(8/7) 당일 로컬 실행 환경 — 동일 compose로 그대로 재현 가능(별도 준비 최소화)

### 6.2 AI 개발 컨벤션 (전원 AI 구현 체제의 품질 통일 장치)

> 상세 실무 규약은 별도 문서 **`AI_개발_컨벤션.md`** 참조 (디렉토리 구조, 코드 예시, 개발 절차, 리뷰 체크리스트 포함)

각 메뉴 담당이 AI 기능을 직접 구현하되, 아래 공통 기반 위에서만 개발한다. AI-LLM 코치가 공통 모듈(`llm_client`)·컨벤션 문서·예시 체인 1개·온보딩 발표자료를 **작성 완료**(2026-07-13 기준) — 온보딩 세션(7/15)에서 배포하고, 코드 리뷰 ①(7/17)에서 준수 여부를 점검한다.

1. **공통 LLM 클라이언트 단일화**: FastAPI에 `llm_client` 공통 모듈(모델명·HF 엔드포인트·타임아웃·재시도·토큰 사용량 로깅 일원화). 모든 체인은 이 클라이언트만 사용
2. **프롬프트 파일 분리**: 하드코딩 금지, `prompts/` 디렉토리에 파일 관리(`report_summary.md`, `defect_explain.md` 등), 공통 시스템 프롬프트 템플릿 사용
3. **출력 형식 통일**: LLM 응답은 JSON 스키마 지정(structured output)으로만 수신 — 자유 텍스트 파싱 금지
4. **엔드포인트·에러 규약**: AI 엔드포인트 네이밍 통일(`/ai/report`, `/ai/chat`, `/ai/nl-search` 등), 요청/응답 공통 포맷, 실패 시 표준 폴백 처리
5. **버전 고정**: LangChain·chromadb 등 requirements 버전 고정 (LangChain은 버전 간 API 변화가 큼). **v0.5 조합: `langchain==0.3.30` + `langchain-core==0.3.86` + `langsmith==0.10.10` + `langgraph==0.6.11`** — 병렬 브랜치가 `langgraph`만 단독 추가하면 pip `resolution-too-deep`이 나므로, 공용 `requirements.txt` 조합을 반드시 동일하게 맞춘다. **`requirements.txt` 변경 PR은 PR봇 자동 머지 제외 — 사람 확인 후 수동 머지**(의존성 설치 = 코드 실행)
6. **법규 문서 청킹**: 임의 글자수 분할 금지 — 법조문 경계 우선 분리자 사용(`RecursiveCharacterTextSplitter(separators=["\n제", "\n①", "\n\n", "\n"])`) — 조문 중간 절단은 출처 인용 정확도(KPI) 저하 직결
7. **AI 오케스트레이션(LangGraph, v0.5)**: 재시도·조건분기가 있는 체인(보고서 생성 FR-5, RAG 챗봇 FR-6)은 손으로 짠 `while`/`if` 대신 **LangGraph StateGraph(노드 + 조건부 엣지)**로 구성 — 사이클이 있으면 `recursion_limit` 명시, 그래프는 모듈 로드 시 1회 compile, 무상태 단발이면 checkpointer 미사용. LangGraph 리팩터는 "테스트 통과"만으로 검증 부족 → 재시도 횟수를 mock `call_count`로, 그래프 구조를 `draw_mermaid()`로 직접 확인(재시도 상한 조용히 축소되는 회귀 방지)
8. **AI 관측(LangSmith, v0.5)**: `get_llm()`이 전 체인의 유일한 LLM 호출 지점이라 환경변수(`LANGCHAIN_TRACING_V2` 등 4개)만으로 자동 트레이싱 — 프롬프트·소요시간·토큰·응답이 LangSmith `hajacheck` 프로젝트에 트리로 기록. docker-compose `fastapi` 서비스에 soft-default로 연결(값 없으면 비활성), 키는 env 주입(코드·로그 평문 금지)
9. **관측 체인 선택적 제외(v1.3, #1534)**: **v1.0~v1.2는 LangSmith 트레이싱 시 개인정보 마스킹**으로 "프롬프트 원문은 노출 차단, 구조·시간·에러는 관측"하는 방식을 취했다. v1.0 차단 방식(체인 트레이싱 자체 제외)의 부작용을 개선해 v1.1에서 "입출력 마스킹"으로 전환했고, v1.2까지 운영해왔다. **v1.3: 마스킹 레이어 대부분 제거, OCR 체인만 완전 제외(#1534, 2026-08-04 P1 반려 반영)** — `tracing_context(enabled=False)`로 OCR 체인(사업자등록증 원문)의 트레이싱을 자체에서 완전히 제외(개인정보 국외 제3자 제공의 법적 근거 불확실). 나머지 5개 체인(report/rag_chat/nl_search/defect_explain/briefing)의 프롬프트 원문(시설명·사용자 질의·하자내용·통계 등)은 **외부 SaaS LangSmith로 그대로 전송**(재봉 승인, 관측 편의 우선). 개인정보를 로그에 평문으로 남기지 않는 원칙(§5)은 유지하되, 트레이싱 경로는 예외

> DL 추론은 AI-DL 담당의 단일 엔드포인트를 호출하는 구조라 별도 컨벤션 불필요.

### 6.3 주요 데이터 모델 — 논리·물리 ERD (v0.5: 구현 스키마 실측 정합, 정본 `docs/design/db/HajaCheck_script.sql`)

> **v0.5 스키마 드리프트 정정**: v0.47까지 "별도 companies 테이블 없이 `company_id` 컬럼만 공유"로 서술했으나, 실제 구현은 **정규 `companies` + `company_memberships` 테이블**로 확정됐다. 또한 dev→main 승격 #531 배포 시 arm1(`ddl-auto=validate`)이 누락 스키마로 부팅 실패한 사고(2026-07-22, 프로덕션 일시 다운) 이후 `lock_version`(낙관적 락)·`company_memberships`·`rag_documents.verification_status` 등을 정합 핫픽스로 반영.
>
> **v1.0 — Flyway 도입 완료(#544)**: 위 드리프트 사고의 근본 차단책이던 Flyway가 **적용 완료**됐다. 이제 스키마 변경은 **`V{n}__*.sql` forward migration만**으로 하며(**v1.1 기준 V1~V40**), `ddl-auto=validate`이므로 엔티티 변경 시 마이그레이션을 누락하면 앱 기동 자체가 실패한다(= #531 재발 방지 장치). 운영 규칙:
> - **한 기능 = 엔티티 변경 + 마이그레이션을 같은 PR**에 담는다(따로 승격되면 validate로 터진다)
> - **기존 마이그레이션 절대 수정 금지** — 이미 적용된 파일 변경은 checksum 불일치로 기동 실패. 변경은 항상 새 번호로만
> - 마이그레이션 검증은 **일회용 DB(testcontainer/로컬)**에서, 확정(리뷰 통과) 전엔 공유 dev DB에 찍지 않는다. 빈 DB·기존 DB 양쪽을 CI에서 검증하는 통합테스트를 둔다
> - Flyway 적용 트리거는 "머지"가 아니라 **"그 코드로 앱이 부팅되는 순간"** — prod 적용은 main 승격 자동배포 재기동 시점

- `users`(소셜 연동 + 기업회원용 `password_hash`·`business_reg_no`·`email`·`email_verified` / `company_id`·`business_verified` / 플랫폼 관리자는 `company_id = null`·역할 `PLATFORM_ADMIN`). **비밀번호 재설정 토큰**은 Redis TTL 키로만 관리한다(별도 토큰 테이블 불필요 — v1.2 정정: v0.45의 "이메일 인증코드"는 미구현)
- **`companies`(기업 — v0.5 실재. `status`·`verification_status`가 `CompanyScopeGuard` 4조건 중 2개 — FR-1. **v1.1: 가입 시 자동승인으로 `VERIFIED+APPROVED` 전이**, 인증 배지는 `ocr_raw.ntsOutcome` provenance로 판정)**, **`company_memberships`(소속·초대·승인 — `status` PENDING/APPROVED/REJECTED/REVOKED/EXPIRED 상태머신, 사용자당 APPROVED 1개 부분 유니크. **v1.1: 가입 시 오너 멤버십 자동 발급** — 이전엔 발급 경로가 없어 prod 전면 403이었다)**, **`user_consents`(이용약관·개인정보 동의)**
- `facilities`(점검 주기 필드 — P1, **소유 주체 = 회사**(#660), **`initial_grade` A~E enum·`assignee_user_id` FK·메모** 필드 확장 — v0.5), `inspections`, `media`
- `defects`(유형, bbox, confidence, 등급, 상태, 검수 여부 + 조치 4필드 **최신 스냅샷**), **`defect_revisions`(검수·조치 수정 이력 — append-only, FR-4 감사 추적/활동 기록)**, **`defect_action_logs`(조치 등록 제출 이력 — append-only, `phase` IN_PROGRESS/RESOLVED, v1.0 신규 — 스냅샷 컬럼이 덮어쓰는 다회 조치 이력 보존)**
- `reports`(DRAFT/FINALIZED 상태, 점검당 유니크 제약, 낙관적 락, `deleted_at` soft delete)
- `chat_sessions`/`chat_messages`(RAG·상담 공용, `session_type` 구분), **`chat_message_citations`(RAG 출처 citation — KPI '출처 표기율 100%' 검증용)**
- `counsel_tickets`(상담 대기열/티켓 — 스냅샷·메모·첨부, 조회 성능용 `created_at` 인덱스), `bot_scenarios`(챗봇 시나리오), `rag_documents`(**`verification_status` UNVERIFIED/VERIFIED — v0.5** / **`embedding_started_at`·`expected_embed_batch` — v1.1 임베딩 진행 상태**, V39·V40)
- **`plans`/`user_plans`/`usage_counters`(멤버십 — P1, §2.4, `lock_version` 낙관적 락, `billing_period`·`payment_pending_until`)**, **`payments`(결제 원장 — 토스 샌드박스, `PaymentStatus` 상태머신, v1.0)**, **`scheduled_plan_changes`(예약 하향 — v1.0)**
- **`notifications`·`inspection_notification_settings`(인앱 알림·점검 알림 설정 — P1, FR-9. 알림 타입에 구독 전이 3종 추가 — v1.0)**
- `api_system_log`(시스템 로그), **`menus`/`menu_role_access`(메뉴·역할 접근 — 현재 `@Entity` 미매핑)**

---

## 7. 팀 구성 및 역할 — 메뉴(도메인) 단위 배정

**팀 목표: 전원이 AI를 활용한 End-to-End 개발자로 성장.** 이를 위해 각 담당자가 자신의 메뉴에 대해 **화면(React) + API(Spring Boot) + AI 연동까지 직접 구현**한다. AI 파트를 특정 담당이 대신 만들어주지 않으며, 기술 영역 담당은 코치·리뷰어로 지원한다.

| 메뉴/영역 | 주담당 | 부담당 | 범위 |
|---|---|---|---|
| 🔐 로그인/마이페이지 | 정재봉 | 오영석 | OAuth2(Kakao/Google), 세션, 프로필·내 이력 화면, **내 플랜·쿼터 체크(P1, §2.4)**, 점검자 좌석 초대 관리, **구독 결제(토스 샌드박스)·예약 하향·미결제 유예 — v1.0**, **사업자등록증 OCR + 국세청 실존확인 연동** |
| 📊 대시보드 + 🏢 시설물 관리 | 허남 | 김관영 | 현황 요약(처리 대기 현황), 시설물 CRUD, 점검 이력 타임라인·**회차 간 변화 비교(P1)**, **점검 주기 설정·도래 표시(P1)**, **알림 센터(P1, FR-9)** + **AI 주간 브리핑 직접 구현**(현황 데이터 → LLM 자연어 요약 카드) |
| 🔍 점검 관리 A — 업로드·분석 | 오영석 | 허남 | 업로드/프레임 추출 연동, **추론 API 연동·비동기 잡 직접 구현**(AI-DL 코치 지원) |
| 🔍 점검 관리 B — 결과 뷰어·검수 ★ | 오영석 | 정재봉 | 오버레이 뷰어(확대·필터), 오탐 수정·등급 조정(휴먼 인 더 루프) + **AI 하자 설명 직접 구현**(선택 하자의 원인·조치 방안 LLM 생성 패널) |
| 📋 하자 관리 + 📈 통계(P1) | 유병현 | 정재봉 | 하자 목록·상세, **하자 상태머신(신규→검수확정→조치중→조치완료) 구현**, 회차 비교, **조치 보드(P2 — 칸반)** + **자연어 검색 직접 구현**(질의 → LLM이 필터 조건 변환, 데이터 질의 단일 창구) |
| 💬 고객지원 (AI 어시스턴트 + 상담) | 이은석 | 김승현 | **RAG 챗봇 파이프라인 직접 구현**(RAG 코치 지원, 법규 Q&A 전담·출처 표시), 시나리오 엔진, **전역 플로팅 위젯**, WebSocket/STOMP 상담(대기열·1:1 채팅·CONNECT 인증) + **(v0.5 이관) 상담 관리·RAG 문서 관리(임베딩·재임베딩 배치)** + **(v1.0) 상담원 전용 콘솔(`/counsel-console` 독립 셸·전용 로그인·스킬 배정) / 플랫폼 관리자 상담 관리는 날짜별 조회·읽기전용 열람으로 재정의** — 상담 내역은 고객지원으로 단일화 |
| 🏠 랜딩 + 📄 보고서 + 🗺 지도 뷰 | 김관영 | 유병현 | **랜딩 페이지**(서비스 소개·요금제 §2.4, **이용약관·개인정보처리방침 라우트** — v0.5), 보고서 편집·PDF 화면 + **LLM 보고서 생성 체인·방어로직 직접 구현**(프롬프트 인젝션 방어·인용 검증·프록시·Rate Limit·DRAFT/FINALIZED — FR-5, LLM 코치 지원), **지도 뷰(P1 — 카카오맵 Geocoder 주소→좌표·등급별 색상, v0.5: EXIF GPS 미채택)** — 독립성 높은 시연 임팩트 화면 3종 "쇼케이스 슬롯" |
| 🛠 관리자 페이지 (모니터링 포함) | 김승현 | 이은석 | PRD 작성·프로젝트 설계·아키텍처 설계, 일정·산출물 관리 + 사용자 관리, **플랜·쿼터 관리(P1)**, 시스템 모니터링·예산 가드레일. **(v0.5) 기업 관리자 / 플랫폼 관리자(`/platform-admin`) 2개 콘솔 분리 + `company_id` 데이터 스코핑**. **(v1.0) 플랫폼 관리자 메뉴 재구성 — 대시보드(옛 서비스 통계) 최상위 개칭·하자 유형·등급 관리 삭제**. ※ **상담 관리·RAG 문서 관리 담당은 이은석으로 이관**(v0.5 — 고객지원 RAG 챗봇 오너와 통합) |

**기술 영역별 담당 — 풀스택 챕터제 (메뉴 횡단 기술 축, 9챕터)**

레이어(FE/BE) 분업이 아닌 **관심사(챕터) 기준** — 모든 챕터가 화면부터 서버까지 관통하므로 누구나 풀스택 경험을 갖는다. 챕터 담당은 해당 기술 영역의 표준·규약을 정하고 코드를 리뷰하는 오너. **메뉴 담당(기능 축)과 매트릭스 교차 운영** — 주/부담당은 그 기술을 실제로 쓰는 메뉴 오너에서 도출. 챕터 9(DevOps)는 특정 메뉴에 속하지 않고 전 챕터를 관통하는 플랫폼 운영 레이어(정재봉 고유 영역).

| # | 챕터 (기술 영역) | 주담당 | 부담당 | 기술 범위 — FE+BE 관통 |
|---|---|---|---|---|
| 1 | 코어 플랫폼 (인증·공통 기반) | 정재봉 | 오영석 | OAuth2·세션·CSRF·권한 매트릭스·`QuotaInterceptor`(BE) + `ProtectedRoute`·XSRF 연동(FE), BE `global/`(ApiResponse·예외 핸들러) + FE `shared/`(axios 인터셉터·공통 컴포넌트·디자인 토큰, **Stitch→Figma**). 양쪽 컨벤션 문서 오너 |
| 2 | 데이터 (DB·API 계약) | 유병현 | 김승현 | PostgreSQL ERD·마이그레이션·인덱스, Redis 키·TTL 규약, dev/prod 분리, JPA Entity 규약 오너 + Swagger 계약 관리·FE 타입 생성·MSW 목서버, 코드 리뷰 ③ Swagger diff 주관 |
| 3 | 실시간 (상담) | 이은석 | 유병현 | STOMP 서버·`CONNECT` 인증(`ChannelInterceptor`)·상담 대기열(Sorted Set)(BE) + 채팅 UI·재연결 훅·연결 상태 표시(FE) |
| 4 | 미디어 파이프라인 | 허남 | 오영석 | 업로드·진행률 UI(FE, **단일 요청+`onUploadProgress` — 청크는 미채택, v0.5**) + 매직바이트(시그니처) 검증·썸네일·재인코딩(BE) + 영상 프레임 추출(FastAPI OpenCV/ffmpeg, **후속 — 현재 이미지 전용**) |
| 5 | AI 추론 (DL) | 정재봉 | 허남 | **클래스별 2모델 학습·추론 서빙 — 균열 U-Net(resnet34) + 박리박락·철근노출 YOLOv8-seg**(Python, **현재 둘 다 raw PyTorch — ONNX+INT8·ONNX Runtime은 후속 목표**, v1.0) + **등급 임계값 캘리브레이션(모델·전처리·집계 조건과 한 묶음 — 조건 변경 시 재측정)** + 잡 상태 API·폴링 규약(BE, **워커 분리는 단일 프로세스 — 후속**) + 오버레이 데이터 포맷 협업(FE). 데이터셋 라벨링 팀 개방 |
| 6 | LLM (생성) **코치** | 오영석 | 김관영 | `llm_client`(HF 엔드포인트·타임아웃·재시도·토큰 로깅)·`prompts/` 규약·structured output·응답 캐시(프롬프트 해시)·**Grounding Check**·`RunnableParallel`·**LangGraph StateGraph 오케스트레이션·LangSmith 트레이싱(v0.5)**(Python) + AI 엔드포인트 envelope(BE) + `AILoadingIndicator`·`AIErrorFallback`(FE). 생성 체인은 각 메뉴 담당 직접 구현, 코치는 리뷰·페어링 |
| 7 | RAG (검색) **코치** | 김승현 | 이은석 | 임베딩 모델(ko-sbert/BGE-m3)·청킹 전략(**법조문 분리자** `\n제`/`\n①`)·Chroma 임베디드 컬렉션·재임베딩 배치 잡·출처 citation 규약(Python) + RAG 문서 관리 화면 협업(FE). 리뷰·페어링 |
| 8 | 프레젠테이션·산출물 (랜딩·보고서·지도) | 김관영 | 유병현 | 보고서 PDF 렌더링·템플릿 조립·편집기·이력(BE+FE) + 지도 시각화(Kakao Map SDK·**Geocoder 주소→좌표** 마커·등급별 색상, P1 — v0.5) + 랜딩·공개 페이지(요금제 §2.4). 챕터6(LLM) 섹션 초안을 받아 편집·출력(생성/출력 분리), 인쇄/차트 스타일 규약 오너(Recharts 3.9.2 공통 컴포넌트 관리) |
| 9 | 플랫폼·운영 (DevOps) | 정재봉 | 오영석 | OCI 인프라 전반(VM·도메인·HTTPS·Nginx)·**PostgreSQL·Redis 설치·보안·공용 dev DB 운영**(§6.1)·GitHub Actions CI/CD·Slack·Jira 셋업·모니터링(메트릭·Correlation ID). 특정 메뉴 도메인에 속하지 않고 **전 챕터를 관통하는 플랫폼 운영 레이어** — 정재봉 고유 영역 |


---

## 8. 일정 (Milestones)

| 기간 | 마일스톤 | 주요 작업 |
|---|---|---|
| 7.9(목)~7.13(월) | **착수보고 초안** (7.13 17:00 제출) | 요구사항·화면설계·ERD 초안, 데이터셋 확보, OAuth 앱 등록. **Contract-First: OpenAPI(Swagger) 스펙 우선 커밋** — 프론트는 MSW 목서버로 병렬 개발 시작, Spring↔FastAPI도 OpenAPI 문서를 계약으로 |
| 7.14(화) | **착수보고 발표** | 1차 발표(초안) |
| 7.15~7.19 | 개발 스프린트 1 | 인프라 셋업, 소셜 로그인/시설물/업로드, 모델 1차 학습. **7.15(수) 전원 AI 온보딩 세션(2~3h)**, **7.17(금) 팀 코드 리뷰 ①**. **스프린트 말미(7/18~19) 워킹 스켈레톤 관통**: 더미 데이터로 로그인→업로드→(더미)탐지→템플릿 보고서 최소 흐름 1회 통과 — 통합 리스크 조기 노출 |
| 7.20(월) | **1차 멘토링** | 아키텍처·모델 방향 점검 |
| 7.21~7.29 | 개발 스프린트 2 | 탐지 파이프라인 연동, 결과 뷰어, 보고서 생성, RAG 챗봇, 상담 챗봇·관리자 페이지. **7.24(금) 팀 코드 리뷰 ②, 7.28(화) 팀 코드 리뷰 ③(통합 전 최종)** |
| 7.30(목) 12:00 | ✅ **중간보고서 초안 제출** | 단위/통합 테스트 병행. (PRD v1.0 기준선 시점) |
| 7.31(금) | ✅ **중간보고** | 멘토 방문, 단위·통합 테스트 완료 |
| 8.3(월)~8.6(목) | 🔄 **사용자 테스트 & 개선** ← **현재 구간 (본 PRD v1.1 기준선)** | 버그 수정, P1 기능, 추가 멘토링(1h). **8.4(화) 팀 코드 리뷰 ④(개선사항 중심)**. 실사용 결함 수정이 이 구간의 주 산출물 — v1.1 변경 이력 §4 참조 |
| 8.7(금) | **완료보고** | 최종 발표·시연 |
| 8.11(화) | 수료식 | — |

**협업 도구**

- **프로젝트 관리: Jira** (PACA 자체 호스팅은 2026-07-07 팀 결정으로 폐기, Jira로 전환) — 스프린트 백로그·태스크 보드·이슈 트래킹을 Jira로 일원화. 태스크는 §7 메뉴/챕터 담당 기준으로 생성, 커밋·PR 메시지에 Jira 이슈 키 연동. 환경 셋업·운영은 DevOps 챕터(정재봉)
- **팀 커뮤니케이션: Slack** — 비동기 소통·알림 채널(신규 도입, PACA 폐기와 함께 전환)
- 코드·리뷰: GitHub (PR 규칙은 아래·컨벤션 문서 참조) / 디자인: Stitch 시안 → Figma 공유 / 문서: 저장소 내 md(PRD·컨벤션)

**코드 리뷰 운영 방식**

- **PR 리뷰(상시)**: main 직접 푸시 금지, 모든 변경은 PR + 부담당(1순위 리뷰어) 승인 후 머지. GitHub Actions CI 통과 필수
- **팀 코드 리뷰(주 1회, 30분~1시간)**: 위 일정 ①~④. 전원 리뷰가 아니라 회차별 주제 1~2개 집중(예: ① API 계약·패키지 구조, ② 핵심 화면·분석 파이프라인, ③ 통합 전 인터페이스 정합 — **API 계약 준수 체크리스트(Swagger diff 리뷰) 포함**, ④ 테스트 결과 반영). 리뷰가 개발 병목이 되지 않도록 시간 엄수

---

## 9. 성공 지표 (KPI)

- 탐지 성능: mAP@0.5 ≥ 0.7 (테스트셋 기준) — **클래스별 mAP 개별 리포트**(전체 평균이 특정 클래스 부진을 가리지 않도록, 데이터 부족 클래스는 P0 제외 판단 근거로 사용)
- E2E 처리: 이미지 100장 업로드→분석→보고서 초안 10분 이내
- 시연 시나리오(로그인→업로드→탐지→검수→보고서→챗봇→상담원 연결) 무결점 완주
- 챗봇 답변 출처 표기율 100%

---

## 10. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 학습 데이터 품질·클래스 불균형 | 탐지 성능 미달 | **AI Hub 데이터셋 약 161,000장 확보 완료**(2026-07-13) — 확보 리스크는 해소. 클래스 3종 확정(균열·박리박락·철근노출)으로 축소, 사전학습 모델 파인튜닝으로 품질·불균형 대응 |
| 기능 범위 과다 (로그인·상담·관리자 추가, **v0.47: 관리자 콘솔 2분리로 범위 추가 확대**) | 4주 내 미완성 위험 | P0 내 우선순위: 분석 파이프라인 > 로그인 > 챗봇 > 관리자. 상담·관리자는 전담 2인 분리 배치. 7/29 시점 코어 불안정 시 상담원 연결은 시나리오 챗봇만 남기고 드롭, **관리자 콘솔은 플랫폼 관리자 전용 기능(기업 관리·전체 통계·시스템 모니터링)부터 P1로 재분류해 드롭 — 기업 관리자 콘솔(FR-8-A)은 P0 유지** |
| Spring↔FastAPI 연동 지연 | 통합 병목 | **Contract-First**: 착수 주간 OpenAPI 스펙 우선 커밋 + 프론트 MSW 목서버 병렬 개발 + **스프린트 1 말 워킹 스켈레톤 관통**으로 통합 리스크 조기 노출 |
| OAuth 심사/설정 이슈 | 로그인 지연 | 착수 주간에 Kakao/Google 개발자 앱 등록 선행 |
| sLLM 생성 품질·API 응답 지연 | 보고서 품질, 데모 품질 | 템플릿 채우기 방식(섹션별 생성→조립), HF PRO 구독/크레딧 확보 |
| **결제 도입으로 인한 범위·리스크 확대**(v1.0 신규) | 금전 사고 가능성, 4주 범위 압박 | **샌드박스(테스트 키)로 한정** — 실 PG 계약·라이브 키는 Out-of-Scope. 키 미설정 시 결제 진입점만 비활성(서비스 전체는 정상 배포)되는 소프트 가드. 라이브 전환은 §11-13 전제 조건(승인 응답 유실 복구 경로·E2E 실검증) 해소 후로 못박음 |
| **등급 임계값과 모델의 암묵 결합**(v1.0 신규 — 실제 발생) | 심각도 과대·과소 경고로 판독 신뢰도 훼손 | 임계값을 "모델·전처리·집계" 3조건과 **한 묶음으로 기록**하고 조건 변경 시 재측정을 규칙화(FR-3). v1 사고는 조건 미기록이 원인이었음 |
| **인가 조건을 "검사하는 코드"만 있고 "충족시키는 코드"가 없음**(v1.1 신규 — 실제 발생) | prod에서 기업 가입 계정이 **회사 스코프 API 전부 403** — 가입 직후부터 기능 사용 불가 | `CompanyScopeGuard` 4조건을 FR-1에 정본으로 명시하고, 가입 트랜잭션에서 **멤버십 발급·회사 승인을 함께 수행**(#1324/#1368). 원칙: 인가 조건은 **검사부와 충족부를 쌍으로** 설계하고, 조건 추가 시 "누가 이 조건을 만들어 주는가"를 반드시 확인한다 |
| **자동승인이 사후 탐지 장치를 무력화**(v1.1 신규 — 실제 발생) | 국세청 장애 구간 가입분이 재검증에서 이탈, 미검증 계정이 영구 통과 | 재검증 대상 판정을 인가 플래그가 아니라 **provenance 화이트리스트의 여집합(fail-safe)** 으로 전환. 인증 배지도 같은 기준 — **증명 못 하면 false**(FR-1) |

---

## 11. 오픈 이슈 (팀 확정 필요)

1. ~~탐지 클래스 최종 범위~~ — **확정(2026-07-13): 균열·박리박락·철근노출 3종**. 클래스별 mAP는 학습 후 §9 KPI로 개별 검증
2. HuggingFace 계정 정책(PRO 구독 vs 크레딧 충전) 및 예산 한도 — LLM 응답 캐시 + 예산 가드레일(FR-8)로 리스크 완화 후 확정
3. ~~심각도 등급 체계~~ — **확정(2026-07-13): 시설물 안전등급 A~E 5등급 준용**(규칙 기반 크기·유형 산정 → 점검자 수동 조정, FR-3)
4. ~~상담원 역할 수행 주체 — 관리자 겸임 권장~~ — **해소(v1.0)**: `COUNSELOR` 역할 + **전용 콘솔(`/counsel-console`) + 스킬 배정**까지 구현되어 "관리자 겸임" 전제 자체가 바뀌었다(상담원은 독립 셸을 쓰는 별개 역할). **잔여 = 상담 운영 시간 정책**(응대 가능 시간·부재 시 오프라인 티켓 SLA)은 여전히 미확정
5. 챗봇 시나리오 초기 구성안 — §1.2의 "사용 문의 / 점검 상담"을 최상위 카테고리로 시작
6. **관리자 콘솔 분리 실행 범위**(v0.47 신규) — 스프린트2(7.21~7.29)에 기업/플랫폼 관리자 2콘솔을 모두 구현할지, 우선 기업 관리자 콘솔만 P0로 완성하고 플랫폼 관리자 콘솔은 §10 리스크 대응대로 P1로 미루며 시작할지 팀 확정 필요. 플랫폼 관리자 계정 시드 방식(초기 데이터 삽입 vs 별도 가입 도구)도 함께 확정
7. **지도 뷰 좌표 확보 방식**(v0.5 신규) — 현재 구현은 시설물 주소를 카카오맵 Geocoder로 변환. PRD 초안의 EXIF GPS 자동추출과 설계가 다름 → **Geocoder 단독으로 갈지 EXIF 병행할지 제품 의사결정 필요**(FR-2·FR-4·§2.3)
8. **ARM CPU 추론 최적화 잔여**(v0.5 신규, **v1.0 전제 변동**) — 현재 raw PyTorch 서빙(ONNX+INT8 미적용). **균열 U-Net 교체로 대상 모델이 2종(U-Net + YOLO)이 되어 ONNX 전환·검증도 각각 필요**해졌다. 전환 착수 시점 + 100장 10분 목표 실측 검증 필요. ⚠️ 전환 시 **등급 임계값 재측정 트리거**에 해당하는지 확인 필요(전처리·집계가 바뀌면 재캘리브레이션 — FR-3)
9. **AI 보고서 법정 표준서식 대응**(v0.5 신규, **v1.1 부분 해소**) — **관공서 표준양식 PDF 1차 적용 완료**(#1294) + 책임기술자 표기(#1409) + 하자 박스 좌표 배선(#1333). **잔여**: 정밀안전점검 표준서식의 서두(제출문·참여기술진 명단)·자료수집 및 분석·안전등급 지정·부록은 미대응 — RAG 컬렉션 적재 및 스키마 확장 여부는 여전히 제품 의사결정 필요(FR-5)
10. ~~보고서 편집기 실사용 검증 블로커~~ — **해소(v1.1)**: v1.0에서 선행 조건(`POST /api/inspections/{id}/analyze`)이 풀렸고, **8/1~8/3 실사용 검증이 실제로 수행**돼 다수 결함이 수정됐다(#1341 저장/확정검증 통합, #1333 박스 좌표, #1409 책임기술자, #1375/#1377 사진·등급 정합). 이슈 종결
11. ~~시설물 팝업 "결과 검수" 버튼~~ — **해소(v1.1)**: 시설물 상세 진입 경로 전환(#1349)과 점검 이력 탭 실 API 배선(#1365), 오버레이 실 bbox 표시(#1370)로 결과 검수 동선이 연결됐다. 이슈 종결
12. ~~content_hash 크로스언어(Python↔Java) 골든 테스트~~ — **완료(#735/#736, 2026-07-24): 실제 검증으로 교체**
13. **결제 라이브 전환 전제 조건**(v1.0 신규) — 현재 결제는 **토스페이먼츠 샌드박스(테스트 키)**이며 실 PG 계약·라이브 키 전환 전에 아래가 선행돼야 한다.
    - **승인 응답 유실 복구 경로**: "첫 승인 요청이 PG에 도달해 실제 승인됐으나 응답만 유실된" 경우, 재시도로는 복구되지 않고(PG가 중복 처리 코드만 반환) 재시도 소진 후 주문이 취소로 닫혀 **실제 청구를 은폐**할 수 있다. 샌드박스에선 실청구가 없어 발동 조건 자체가 없으나, 라이브에서는 "돈은 받고 요금제는 미지급 + 원장은 취소" 상태가 된다 → **라이브 전환 전 필수 해소**
    - **결제창 E2E 실검증**: 현재까지 검증은 테스트 코드·목서버 기준이며 실제 결제창→리다이렉트→승인 전 구간을 통과시켜본 적이 없다(successUrl/failUrl 수용 여부, 실 승인 응답 필드와 DTO 매핑 일치)
    - 정기 자동결제(빌링키)·환불·대사(정산 대조) 범위 확정
14. **하자 등급 임계값 DB화 시점**(v1.0 신규) — 임계값은 현재 상수 모듈 하드코딩이고, DB화 UI로 예정돼 있던 "하자 유형·등급 기준 관리" 메뉴는 v1.0에서 **삭제**됐다. 재측정이 모델·전처리·집계 조건에 묶여 있어 운영자 임의 변경이 위험할 수 있으므로, **DB화 여부 자체를 재검토**(운영 편의 vs 캘리브레이션 정합성)
15. **기업 계정 킬스위치 5종**(v1.1 신규, 후속 #1367) — 가입 자동승인(FR-1) 도입으로 "가입 = 즉시 전권"이 됐으므로, 잘못 열린 계정을 **닫을 수단**이 필요하다. 아래 5개가 미비 상태다.
    - **사업자 소유·통제 미검증** — 국세청 API는 사업자의 **실재**만 확인한다. 타인 사업자번호로 가입하는 **사칭은 어떤 자동 통제로도 탐지 불가** → 수단 부재로 확정 수용된 범위, 운영 절차로 보완할지 결정 필요
    - **BRN 선점 락아웃** — 사칭 가입이 먼저 등록하면 진짜 사업자가 가입 불가
    - **운영자 수동 차단 API 부재** — 현재 코드 경로로 회사를 차단할 방법이 없다(수동 SQL만)
    - **`/api/admin/**`이 `CompanyScopeGuard` 우회** — 차단된 회사의 ADMIN도 사용자·플랜 관리 접근 가능(FR-1)
    - **FAILED 확정 경보·복구 왕복 부재** — `FAILED`는 재검증에서 영구 제외라 오판이 자가치유되지 않는다
16. **균열 폭·길이 mm 실측 환산**(v1.1 신규) — 픽셀 측정값(`width_px`·`length_px`)은 구현됐으나 **mm 환산은 사진 속 기준물 없이는 불가**하다(640 입력 1px≈1.5mm, 법적 기준 0.3mm = 0.2px 서브픽셀). 업계 기준(허용균열폭 0.3mm)과 A~E 등급을 잇는 **유일한 경로**라 보정 데이터가 필요하다 — 촬영 규약안 `docs/design/crack-measurement-protocol.md`(v0.1, 오영석, **상태: 제안 — 팀 합의 필요**). 요구 수준별로 70~80장(라벨만) / 30장 이상(기준물 부착) / 20~30장(균열자 실측)으로 단계 구분. **촬영 착수 여부·담당·일정 확정 필요**
17. **area_ratio의 프레이밍(촬영거리) 의존성**(v1.1 신규 — 미해결로 확정) — 같은 균열도 촬영거리에 따라 area_ratio가 1.5~1.8배 변한다. **거리 보정·밴드 확대 두 대안 모두 실측으로 기각**됐다(FR-3). 현재는 "촬영 조건이 유사하다"는 전제를 깔고 운영하며, 근본 해결은 위 16번(mm 환산) 경로에 종속된다 — **제품 차원에서 이 한계를 사용자에게 고지할지** 결정 필요(보고서 고지 문구 등)
18. **V38 소급분 재검증 쿼터 상시 소모**(v1.1 신규) — V38로 소급 승인된 기존 회사는 provenance가 `UNKNOWN_BACKFILL`이라 **증명 불가 = 재검증 대상**으로 상주하며 국세청 일일 쿼터를 계속 소모한다. fail-safe 여집합 설계 자체는 의도된 것이므로, 배포 후 **배치 로그(대상·SKIPPED 건수) 모니터링**으로 판단하고 완화 정책은 #1367 범위에서 검토
