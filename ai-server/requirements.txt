# 버전 고정 — 수정은 AI-LLM 코치 승인 필수 (AI_개발_컨벤션.md §7)
# 정확한 버전은 온보딩 세션(7/15) 전 코치가 확정·공지
python-dotenv==1.0.1
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
redis==5.2.1
langchain==0.3.30  # 0.3.13→0.3.30: langgraph 0.6.11과 호환 가능한 최신 버전 (WS1 PR #703과 동일 조합)
langchain-core==0.3.86  # langgraph 0.6.11 호환(0.3.26+ 요구) — 명시 고정 안 하면 pip가 langchain==구버전과의
                        # 호환 버전을 탐색하다 resolution-too-deep으로 실패한다(CI 실측, PR #710 회귀)
langchain-chroma==0.1.4  # chromadb 0.5.23과 호환, langchain-core<0.4 만족
langchain-huggingface==0.1.2
langsmith==0.10.10  # langchain-core>=0.3.26 요구에 따라 upgraded (WS1 PR #703과 동일 조합)
langgraph==0.6.11  # StateGraph 기반 체인 구현용 (WS1: report_chain, WS3: rag_chat_chain 공용)
chromadb==0.5.23
sentence-transformers==3.3.1
rapidocr==3.9.2  # 사업자등록증 OCR(HAJA-169/#552, EasyOCR→RapidOCR PP-OCRv5 한국어 교체 #722)
                 # — 실측 완전일치 3/20(EasyOCR)→18/20, 추론 4장 5.8s→1.0s, 모델 ~95MB→13MB
onnxruntime==1.27.0  # rapidocr 추론 백엔드. manylinux_2_28_aarch64 휠 확인됨(arm1 소스빌드 OK, #722)
ultralytics==8.3.63  # 하자 탐지(dev-05-04) — YOLOv8-seg 추론. torch/torchvision은 Dockerfile에서 이미
                      # CPU 전용 인덱스로 먼저 설치돼 있어 이걸 재사용한다(rapidocr와 동일 전제).
                      # huggingface_hub는 langchain-huggingface의 기존 전이 의존성을 그대로 사용(신규 pin 없음).
