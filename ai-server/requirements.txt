# 버전 고정 — 수정은 AI-LLM 코치 승인 필수 (AI_개발_컨벤션.md §7)
# 정확한 버전은 온보딩 세션(7/15) 전 코치가 확정·공지
python-dotenv==1.0.1
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
redis==5.2.1
langchain==0.3.30  # 0.3.13→0.3.30: langgraph 0.6.11과 호환 가능한 최신 버전 (WS1 PR #703과 동일 조합)
langchain-core==0.3.86  # langgraph 0.6.11 호환(0.3.26+ 요구) — 명시 고정 안 하면 pip가 langchain==구버전과의
                        # 호환 버전을 탐색하다 resolution-too-deep으로 실패한다(CI 실측, PR #710 회귀)
langchain-chroma==0.1.4  # chromadb 0.5.23과 호환, langchain-core<0.4 만족
langchain-huggingface==0.1.2
langsmith==0.10.10  # langchain-core>=0.3.26 요구에 따라 upgraded (WS1 PR #703과 동일 조합)
# 버전 bump 시 tests/test_langsmith_tracing_exclusion.py 필수 재검증 — langsmith 내부 API
# (run_trees._CLIENT, Client.request_with_retries kwargs 구조)에 직접 의존한다 (PR #1557 P3).
langgraph==0.6.11  # StateGraph 기반 체인 구현용 (WS1: report_chain, WS3: rag_chat_chain 공용)
chromadb==0.5.23
rank_bm25==0.2.2  # regulations 컬렉션 하이브리드 검색(벡터+BM25 RRF, #1410). 순수 파이썬 소구현
                  # BM25Okapi 하나만 제공하는 경량 라이브러리 — 마지막 릴리스(2.2)로 고정.
kiwipiepy==0.23.2  # ai/core/bm25_index.py 한국어 형태소 분석 토큰화(#1410). 정규식 토큰화는
                   # 조사·어미가 붙어 BM25 recall이 낮음을 law.go.kr 공식 법규 PDF 7건 실측(질의
                   # 27개)으로 확인 — kiwipiepy 교체로 vector-only 대비 recall@10/MRR/nDCG 개선.
matplotlib==3.10.0  # ai/eval/run_eval.py 벡터-only vs 하이브리드 지표 비교 막대그래프 저장용(#1410).
                    # 평가 하네스 전용(로컬/수동 실행), pytest 대상 아님.
sentence-transformers==3.3.1
rapidocr==3.9.2  # 사업자등록증 OCR(HAJA-169/#552, EasyOCR→RapidOCR PP-OCRv5 한국어 교체 #722)
                 # — 실측 완전일치 3/20(EasyOCR)→18/20, 추론 4장 5.8s→1.0s, 모델 ~95MB→13MB
onnxruntime==1.27.0  # rapidocr 추론 백엔드. manylinux_2_28_aarch64 휠 확인됨(arm1 소스빌드 OK, #722)
ultralytics==8.3.63  # 하자 탐지(dev-05-04) — spalling/rebar_exposure 전용 YOLOv8-seg 추론(유형별
                      # 개별 체크포인트, #953/#962 이후 HF Hub 저장소 구조 변경 — yolo_client.py 참고).
                      # torch/torchvision은 Dockerfile에서 이미 CPU 전용 인덱스로 먼저 설치돼 있어
                      # 이걸 재사용한다(rapidocr와 동일 전제). huggingface_hub는 langchain-huggingface의
                      # 기존 전이 의존성을 그대로 사용(신규 pin 없음).
clip @ git+https://github.com/ultralytics/CLIP.git@488e81a6711eea7346872b46ea928b367da8889d  # YOLO-World 인코더 의존성(#1487 카드검출), 커밋 고정(#1547 P2)
segmentation-models-pytorch==0.5.0  # 균열(CRACK) 전용 U-Net(resnet34) 추론(dev-05-04, 2026-07-27).
                      # crack_unet_resnet34.pt(HF Hub) state_dict가 이 버전의 Unet(encoder_name=
                      # "resnet34") 구조와 strict 로드로 정확히 일치함을 직접 검증 후 고정(레이어명이
                      # 버전마다 달라질 수 있어 스모크 테스트 없이 올리면 위험 — ai/core/unet_client.py 참고).
                      # torch는 위와 동일 CPU 인덱스 재사용, timm(인코더 프리트레인 가중치 레지스트리)이
                      # 전이 의존성으로 함께 설치됨(encoder_weights=None이라 실제 다운로드는 없음).
