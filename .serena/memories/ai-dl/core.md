## ai-dl (모델 학습/트래킹) — 세부

- 과거 `ml/` 폴더명이었으나 PRD 챕터5 용어 통일로 `ai-dl/`로 리네임됨(ee69ef1) — 옛 문서/커밋 참조 시 `ml/` 경로는 stale.
- `tracking.py`: MLflow 실험 추적 공용 헬퍼(챕터9 모니터링). 트래킹 경로가 cwd 의존이던 버그 수정 이력 있음(e0f02a5) — cwd 가정하고 경로 하드코딩하지 말 것. Colab 세션 유실 대비 가이드가 파일 내 주석으로 포함됨.
- `example_train.py`: 학습 스크립트 예시(팀원 참고용).
